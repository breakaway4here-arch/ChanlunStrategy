# 项目审查修复：发布执行说明

本说明对应 R01—R10 候选修复。目标是完成可发布的代码、验证、精确数据迁移及恢复准备；实际部署和正式数据执行另行记录，不能用准备完成代替已经上线。

## 固定范围

- 代码基线：`0e5225ea8351b7edc635368dbcb0aa39b0fd8586`，2026-09-12 已重新 fetch 核对 `origin/main`。仅使用隔离工作树 `codex/project-audit-fixes-20260911` 的候选文件清单，保留主目录原有改动。
- 正式库已有本轮 206 个 BJ 身份及 27788 条可信新日线，旧日线已归档；覆盖为 `5365/5831=92.01%`。本次不再扩量取数、不要求剩余缺口全部消失。
- 待执行的数据批次固定为原联合清单扣除已应用的 78 个尾部清理项，共 **57822 项**：39978 个错误 SH 股票身份行情删除、7020 个有来源的 SZ 价格重建、10504 个有来源的日线数量修复、320 个无可信替代的旧分钟记录清理。最后一类保留数据缺口，不补造分钟确认。
- 待执行清单：`research/project-audit-fixes-20260911/release-readiness-20260912/remaining-repair-formal-candidate-v1.jsonl`，SHA256 `11204828245d583b7729bd9c6aa4fa3a390c0f028f9ccbe57ecc29578b573423`。仅更换当前基线和剔除已完成的 78 项，没有引入新股票、推断值或供应商请求。
- 对应正式库 SHA256：`32553c870c569eb3880591cb2229f346273c68525a0a7216d485b9dc91f32851`。正式库路径为 `/Users/yangfan/yf_source/ChanlunStrategy/.cache/chanlun/market_history.sqlite`。若发生漂移，停止执行并重新核对旧值；不得跳过校验或直接改清单哈希来放行。

## 发布前证据

最新证据目录为 `research/project-audit-fixes-20260911/release-readiness-20260912/`：

- `remaining-repair-rehearsal-v2.json`：当前正式库快照中的 57822 项旧值、新值、重复执行和精确回退通过；范围外日线、分钟及其他原有表未变，206 个 BJ 身份及归档保持不变，覆盖仍为 92.01%。
- `remaining-consumer-impact-v2.json`：同一资格函数和参数，名单 3481→3481，三种检索排序完全一致。历史输入中的 8022 个成交量差异均与清单相符；当前返回特征只有 002155 的 `volume_ratio_3v10` 从 `1.4001366886402147` 变为 `1.4001366405249613`，检索分数与顺序均不变。该证据不是交易收益回测。
- `formal-candidate-readonly-preflight-v1.json`：使用正式路径受限适配器，对实际正式库只读检查全部 57822 项通过；没有应用。
- `current-combined-suite-v1.json`：同步基线后完整 2188 项通过，0 失败、0 错误、0 跳过；包括 11 日冻结输入。后续仅迁移适配器集成发生代码变化，其最终相关验收另见发布清单。
- `current-worker-contract-v1.json` 与 `current-worker-typecheck-v1.json`：实际接口处理及 DurableObject 方法的 8 项本地 SQL 适配验证通过，类型检查通过；没有启动 Cloudflare 模拟服务。云端平台行为仍须部署后回查。
- `validation-scripts/` 保存了阻断网络、监听和正式 SQLite 的完整 Python 回归入口。入口脚本按相邻文件定位 guard，不再依赖遗漏的 `/private/tmp` runner。
- 页面源文件与此前真实 Chrome/ECharts 桌面、390/320 宽及键盘验收的字节哈希一致；历史日报内嵌事实保持原样。正式库补数不会被伪装成旧日报已按新数据重新生成。

## 执行次序和授权

1. 再次确认候选清单、最新目标分支祖先关系及实际运行目录状态，检查没有其他任务正在写相关正式数据。不能通过启动服务或直接运行 `daily_run.sh` 探测状态。
2. 明确取得**剩余 57822 项正式数据库写入**授权。此前 92% 补齐的写入范围只有 78 个尾部清理项与 206 个身份窗口；本说明和预演本身不扩展该授权。为执行准备一致备份、旧值条件和唯一回退文件，不复用旧批次回退文件。
3. 用候选 `scripts/apply_market_history_repair.py` 执行上述固定清单。默认只读；真正写入必须同时给出 `--apply --allow-formal-db`、与唯一白名单一致的 `--authorized-formal-path`、本批用户授权引用 `--authorization-reference` 和新的 `--rollback-out`。不得用临时路径别名绕过正式路径检查。
4. 先部署兼容 `preclose-1445-v2/v3` 的 Worker，再切换 Python 运行版本。整理提交时显式包含清单内的未跟踪源文件及受 Markdown 排除规则影响的 AGENTS/方案文档；不能把整个 research 目录或用户原有脏文件加入提交。
5. 在实际运行目录仅执行经过核对的版本更新，核对代码和资产哈希；静态页面使用同一已验收资源发布。再检查在线资源、桌面/手机展示及既有公开接口。新日报交由原有合法调度窗口生成，不为补验收重复推送旧日报。
6. 正式库应用后逐项读回、检查范围外不变、复核 92.01% 完整分母与实际消费者，记录真实回执。代码、Worker、页面、日报和实际接收各自记录结果。

## 恢复与兼容性

- 当前旧运行代码仍将 `92xxxx` 路由到 SZ，而正式库的 206 个身份已经迁为 BJ。候选修复了该路由；切换前不能把旧运行与新身份组合当作已验收版本。
- 数据工具按旧值条件应用，按新值条件反向回退；后续数据被修改时拒绝覆盖。当前批次已在快照中完整验证精确回退。恢复文件持久保存后才提交数据库事务。
- **不得直接回退源码到旧版后继续正式输出。** 旧路由及旧数量消费与新数据可能不兼容。回退时先保持正式输出不可用，保留已修数据，再使用经验证的兼容版本；如果数据本身错误，按对应批次的独立授权及反向校验恢复，不直接拿旧备份覆盖后续数据。
- Worker 回退必须保留在线 Python 使用的版本兼容性。任何无法确认的回退组合保持不可发布，不以旧版本历史可用代替当前数据兼容性验证。

## 当前执行状态

2026-09-12用户确认“发布吧”后，本说明的固定批次已执行：57822项正式修复及读回通过，代码提交a1aa108a和页面投影补发提交da6d4b63均已进入main和实际运行目录；Worker版本为20db94c5-1ed4-4150-8d4e-f109aa4b34d9。GitHub Pages部署成功，实际桌面、390/320宽手机、键盘跳转及候选K线验收通过。原始日报JSON未重写，没有重跑或重复推送旧日报；下一次自然调度的业务结果仍需按届时实际记录判断。完整回执保存在`research/project-audit-fixes-20260911/release-execution-20260912/`。
