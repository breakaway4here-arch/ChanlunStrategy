# Chanlun Frontend Optimization Landing Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Restructure the v2 daily report so the header answers market context, tabs keep candidate counts, and the auxiliary area becomes a structured decision center.

**Architecture:** Keep the existing v2 shell and workspace model. Implement the optimization only in the frontend assets, then copy assets into `docs/assets` through the existing report generator workflow. Do not change candidate ranking, `workspace.views`, `watch_score`, `action`, or `risk_flags` semantics.

**Tech Stack:** Plain JavaScript in `chanlun/report_assets/report-v2.js`, CSS in `chanlun/report_assets/report-v2.css`, Python unittest coverage in `tests/test_report_generator.py`, static docs generated by `chanlun.report_generator.generate_report`.

---

## Source Spec

- `/Users/yangfan/Downloads/chanlun_frontend_optimization_landing_plan.md`

## Confirmed Scope

- Do: replace duplicated header pool-count chips with market overview.
- Do: keep pool counts only in workspace tabs.
- Do: rebuild auxiliary information as structured decision cards.
- Do: keep the first version display-only; frontend-derived market status is allowed.
- Do not: change `chanlun/report_view_model.py`.
- Do not: change recommendation ordering, scoring, risk logic, or source pools.
- Do not: introduce new dependencies or change access control/chart lifecycle.

## Task Split

### Task 1: Header + Decision Center JS

**Files:**
- Modify: `chanlun/report_assets/report-v2.js`
- Test: `tests/test_report_generator.py`

**Steps:**
1. Add market summary helpers:
   - `getMarketItems`
   - `buildMarketSummary`
   - `buildMarketStyleHint`
   - `renderMarketRegime`
   - `renderMarketIndexCards`
2. Update `buildAppShell()`:
   - Header class becomes `report-header market-header`.
   - Auxiliary section class becomes `aux-center decision-center`.
   - Auxiliary summary title becomes `辅助决策中心`.
   - Details can be `open` by default if layout remains acceptable.
3. Update `renderHeader()`:
   - Subtitle remains date + `交易观测台`.
   - Header content shows market status, operation pace, market breadth, average change, and index cards.
   - Remove top header pool-count chips for `看点 / 主推 / 加速 / 罗姐池 / 等确认 / 基准`.
4. Replace old auxiliary `ul/li` rendering with decision cards:
   - `renderDecisionCard`
   - `renderStatusBadge`
   - `renderMarketTemperatureCard`
   - `renderSectorFlowCard`
   - `renderLimitUpCard`
   - `renderEventsCard`
   - `renderSellSignalsCard`
   - `renderRecentReviewsCard`
   - `renderDiagnosticsCard`
5. Add or update lightweight tests in `tests/test_report_generator.py`:
   - Asset JS contains the new helper names.
   - Asset JS contains `辅助决策中心`.
   - Asset JS no longer contains the old top chip string `metric-chip">看点`.
   - Asset JS still renders workspace tab counts.

**Run:**
- `node --check chanlun/report_assets/report-v2.js`
- `python3 -m unittest tests.test_report_generator -v`

### Task 2: Header + Decision Center CSS

**Files:**
- Modify: `chanlun/report_assets/report-v2.css`

**Steps:**
1. Replace header metric-chip layout with market header styles.
2. Add market regime card and market index card styles.
3. Add decision center, decision card, status badge, flow row, signal row, event row, review row, and diagnostic row styles.
4. Preserve existing workspace tabs and mobile drawer behavior.
5. Add responsive styles:
   - Market index cards become 3 columns under `980px`, 2 columns under `760px`.
   - Decision cards become single column on smaller screens.
   - Tabs remain horizontally scrollable.

**Run:**
- `node --check chanlun/report_assets/report-v2.js`
- Visual inspection after docs regeneration.

### Task 3: Integration, Regeneration, Review, Verification

**Files:**
- Modify via generator: `docs/assets/report-v2.js`
- Modify via generator: `docs/assets/report-v2.css`
- Possibly modify via generator: `docs/index.html`, `docs/2026-06-29/index.html`, `docs/data*.json` only if current generation does so.

**Steps:**
1. Review worker changes for:
   - No `report_view_model.py` edits.
   - No algorithm changes.
   - Access control helpers unchanged except call sites if necessary.
   - Chart lifecycle unchanged.
2. Regenerate docs from `docs/data/2026-06-29.json`.
3. Verify `chanlun/report_assets/*` and `docs/assets/*` match.
4. Run tests:
   - `python3 -m unittest tests.test_report_view_model tests.test_report_generator tests.test_requests_sessions tests.test_market_data_guard -v`
   - `python3 -m py_compile chanlun/report_generator.py chanlun/report_view_model.py`
   - `node --check chanlun/report_assets/report-v2.js`
   - `git diff --check`
5. Browser verify:
   - Desktop header shows market cards, not pool-count chips.
   - Tabs still show counts and switch views.
   - Auxiliary center shows seven decision cards.
   - Mobile has no horizontal page overflow; candidate click opens detail drawer.
6. Commit and push.

## Acceptance Checklist

- Header contains market status, operation pace, breadth/average, and index cards.
- Header does not repeat pool counts.
- Tabs keep counts.
- Auxiliary center has `市场温度`, `板块资金`, `涨停情绪`, `事件驱动`, `卖出提醒`, `策略回看`, `数据诊断`.
- Empty states are friendly and visible.
- Sell signals are visually risk-toned.
- No candidate ranking or workspace algorithm changes.
