# 2026-09-14 板块完整性误判修复

## 本批目标与授权

用户在根因确认后授权“新分支修改”，并要求合并 main 时注意并行任务 `01a09ecd-61f5-75c1-ac4d-23d16dba6bd6`。本批恢复 A/B 股混合板块的正确取齐判断，保留 A 股筛选、真正漏页时的阻断，以及原有股票名单和策略逻辑。

主进程保持当前配置，负责方案、固定输入对照、独立审查与合并；用户已确认落地子 agent 使用 GPT-5.6 Sol / medium、Fast 关闭。此次代码修复与合并不自动执行正式日报重跑、通知或历史补数。

## 基线、工作树与并行任务

- 初始 main / production-runtime：`a0edd61db111ca47eab59659a75a11fe0389bca0`。
- 实施树：`.worktrees/sector-completeness-20260914`；分支：`codex/sector-completeness-20260914`。
- 根目录仍为 `f649af62aad39732cd9ff3f5d2e94f045d9c44d8`，保留已有未提交改动。运行树已有 9 月 14 日日报产物，同样保留。
- 读取根目录、运行树与实施树 `AGENTS.md`，规则 SHA256 均为 `3dccacd0290ef3c49c0ea8921a31240d8a3e86c1aa785ab202c9f7f4b74785a4`；已确认实施树规则与选股修复方案受 Git 跟踪。
- 并行任务“Review上线证据包并优化”使用 `.worktrees/postrelease-review-a0edd61-20260914`，修改比较区文案、前端测试和生成页面。本批产品范围限 `chanlun/data_fetcher.py` 与完整性测试。共同方案文档如有重叠，按实际最新版本追加合并，禁止用旧版覆盖。
- 提交前和合并前分别同步 origin/main，确认祖先关系；若并行提交已进入 main，先合入并重做相关回归。禁止强推、覆盖、stash 或误提交其它任务内容。

## 根因与最小修复

9 月 12 日提交 `a1aa108a7e2711dccea593ce07b1e2b0a4ef9da0` 在 `fetch_sector_stocks` 中加入 A 股过滤，却继续用过滤后的集合数量与接口原始 total 比较。9 月 14 日 15:05 首跑和 15:20 重试均生成报告，随后因 `stock_pool_incomplete=true`、`is_official=false` 被发布校验拒绝。

逐页核实六个异常板块的返回记录与原始唯一代码均等于接口 total，差额全部是 B 股：

| 板块 | 原始总数 | 筛选后 A 股数 |
| --- | ---: | ---: |
| BK1216 | 511 | 507 |
| BK0545 | 259 | 257 |
| BK0438 | 130 | 128 |
| BK0546 | 17 | 15 |
| BK1208 | 78 | 75 |
| BK0737 | 134 | 133 |

最小实现为：维护过滤前的有效代码去重集合，用于分页完整性和进展判断；A 股结果集合继续按既有规则生成。不能用原始行数达到 total 作为取齐证明，重复数据、缺代码、坏行、空页、请求失败和上限保护仍须有效。保留原有发布门，不直接修改任何报告的 official 标志。

## 影响范围与验收

- B03：取齐证据与筛选结果数量分开，不能把过滤解释成漏取。
- B06：诊断数量忠实于原始记录和筛选结果。
- B08：检查共用取数入口的正式与预跑相关回归。
- B09：仅隔离测试与代码合并；不写正式数据库、账本或报告，不重跑、不通知。
- B10：保存引入提交、固定输入前后结果、失败与通过测试、独立审查及合并版本。
- B01 历史补数、B02 价格尺度、B04 策略身份、B05 生命周期、B07 评分排序均未修改，相关旧保护继续有效。

验收覆盖纯 A 股正常分页、A/B 混合单页、完整 B 股页后继续取 A 股、完整 B 股板块、重复页、真实缺页和畸形行；补充取数至官方状态判断的隔离集成验证。先证明新增反例在旧版失败，再验证修复通过。

主进程将用冻结的六个异常板块和一个正常板块逐项对照：A 股名单及字段应保持一致，仅已取齐的混合板块完整性状态恢复；不做策略参数、收益或正式排序变更，也不宣称收益改善。

本地证据目录：`/private/tmp/chanlun-sector-completeness-20260914`。固定接口数据 `frozen-sector-payloads.json` 共 7 板块、16 页，SHA256 为 `5a19c3fb5aad03baaadfad6eb192471f9b9a0d731d92bb6885fcb7468fb2abb8`。

## 执行结果

实现仅调整原始有效代码去重、完整性及进展判断，新增 `raw_valid_unique`、`filtered_unique` 诊断；兼容字段 `unique` 继续表示筛选后 A 股数量。原始有效代码限定为 ASCII 六位数字，完整 B 股页可以继续读取后续 A 股页。

验证结果：

- 子 agent 修改前基线：152 项通过。新增用例在旧代码上出现混合板块、B-only、后续页和质量状态的预期失败；最终唯一 159 项通过。
- 主进程单独在内存中加载基线实际函数，3 项行为反例全部失败且无执行错误；移除 ASCII 检查后，无效数字代码反例单独失败。过程未修改运行源码或正式产物。证据：`parent-negative-proof.log` / `.json`。
- 主进程最终相关回归：`tests.test_market_data_guard`、`tests.test_identity_contract`、`tests.test_preclose_data`、`tests.test_preclose_pipeline`、`tests.test_daily_run_script`，159 项通过，退出码 0。证据：`parent-final-regression.log`。
- 7 板块、16 页固定真实输入回放通过，6 个异常板块 `complete=false→true`，正常板块保持 `true`。筛选后所有 A 股记录的字段、数量、顺序及请求页均逐项一致。证据：`fixed-input-comparison.json`。
- `git diff --check` 通过。本批没有启动服务或监听端口，没有写入正式数据库、账本或报告，也没有发出通知。

独立 Sol / medium 审查未发现 P0/P1 阻断，另行执行定向 15 项和取数保护模块 100 项均通过。主进程已确认本批四个文件的暂存范围，显式强制纳入受本地 Markdown 排除影响的新计划文件，并确认规则及两份计划受跟踪。

主进程据上述证据批准本批代码合并；远端合并版本在交付时回读。上述验证不表示 9 月 14 日正式日报已经重新生成或发布。
