# 2026-09-14 罗姐研究池部分数据发布修复

## 授权与边界

用户要求在 main 重跑当日日报。首次补跑已恢复板块完整性和正式输入校验，但发现罗姐研究池保存与发布校验的独立兼容问题。主进程说明方案后，用户明确回复“授权最小修复并继续发布”。实现继续使用 GPT-5.6 Sol / medium、Fast 关闭；主进程保持当前配置，负责独立验收、合并 main、同步实际运行目录和正式重跑发布。

本批保留已核验的部分可用研究数据及缺口说明，不修改策略参数、选股名单、正式排序或研究身份。不得以清空研究池、伪造完整状态、关闭正式保护或无限补数完成发布。

## 基线与证据

- 远端 main 和实际运行树 `.worktrees/production-runtime`：`5ad058c08a5d89bbca315964476c8cca6b053281`，已包含板块修复 `8563adee` 和并行任务的比较区文案发布。
- 新实施树：`.worktrees/luojie-partial-publish-20260914`；分支：`codex/luojie-partial-publish-20260914`。
- 已核对根目录和实施树规则，`AGENTS.md` SHA256 为 `3dccacd0290ef3c49c0ea8921a31240d8a3e86c1aa785ab202c9f7f4b74785a4`，规则与选股方案受跟踪。其第 12—14 节保护继续有效，历史发布权限与轮次不移作新授权。
- 16:16:55 正式补跑由现有 LaunchAgent 执行。原始产物与续跑记录已备份；依据并行任务的逐文件备份，验证首页仅替换资源版本及提交内 JS 后，在发布锁下更新了续跑日志版本。未改变报告数据或绕过来源校验。
- 当日新报告 `data_quality.is_official=true`、`stock_pool_incomplete=false`，正式输入为 `verified`。罗姐 15 分钟请求 60 只，已核验 40 只、缺失 20 只；26 只候选全部在已核验集合中，与缺失集合无交集。
- 发布错误：`luojie_pool pool contract invalid: 15分钟研究输入部分核验，保留已核验候选并标注缺口`。首次补跑退出码 1，未提交或发布；PID 44573 / 44724 及进程组 44573 已确认退出，发布锁已释放。
- 冻结证据位于 `/private/tmp/chanlun-sector-completeness-20260914/rerun-20260914/attempt1`；包括当前报告、首页、当日归档和来源日志。修复验证另外保存在 `/private/tmp/chanlun-luojie-partial-publish-20260914`。

## 根因与最小方案

1. `run._finalize_luojie_research_pool` 已生成 `status=partial`、`mode=partial`，后续附有逐股 `input_health` 和研究版本。`_serialize_luojie_pool` 只保存 mode、reason、params、diagnostics、candidates，遗漏状态、健康证据及版本，导致合法部分输出保存后失去必要合同。
2. `resolve_nested_strategy_pool` 已有基于逐股核验的部分可用解析，但 `validate_report_contract` 无论其 `contract_valid` 是否有效，均把 partial 作为整份正式日报错误。

实现应保留生产者提供的状态、健康信息和版本，不能给缺失证据填默认“已核验”。发布校验仅对罗姐研究池的合同有效 partial 分项允许发布：数量一致、候选有逐股核验证据、与缺失集合无交集，且不得借解析时过滤掩盖原始未核验候选。缺证、冲突和正式池错误继续阻断。

不顺便修改 H4、主推、次日大涨或其它策略的准入保护；不调整研究预算和正式字段。部分可用仍显示部分可用，已有候选继续作为研究观察。

## 验收范围

- 生产者 → 保存 → 读取 → 解析 → 正式发布校验的往返反例：先在旧版失败，再在修复后通过。
- 有效 partial 保留研究候选及证据，完整／关闭／正常无信号路径不退化。
- 缺 verified_codes、计数不一致、核验与缺失交叉、缺状态、混入未核验候选保持拒绝。
- H4 partial、正式健康失败等负例保持拒绝；同一固定输入的 main / H4 正式视图及候选内容不变，不宣称收益改善。
- 相关生成器、市场保护、研究池、预跑及日报入口回归；独立审查实际 diff 和关键结果。
- 提交、合并前同步最新 origin/main，保留并行任务和根目录脏改动。发布前核对实际运行树及来源日志，使用既有发布锁、校验、账本固化、提交及推送入口。
- 以线上首页日期、归档、数据清单及 JSON 与正式提交一致确认当日报告发布；本地生成或 Git 推送不能替代线上回读。

B03/B06：覆盖与事实证据完整保留；B04：研究身份不升级；B08/B09：入口与正式隔离保护；B10：固定输入、负例和发布回执。B01 历史补数、B02 价格尺度、B05 生命周期、B07 评分排序未修改。

## 执行结果

实现限于 `report_generator.py`、`validate_today_report.py` 及新增专项测试；共享 `pool_contract.py` 保持基线。生产者字段按原值保留，缺失状态不补默认“已核验”；罗姐专用发布判断检查状态一致、日期、研究许可、双份健康证据、清单唯一性、缺失交集和原始候选无损保留。

已取得的验收证据：

- 专项 10 项通过；旧实现已复现状态冲突误放行、双 health 非集合字段异常、重复证据及原始候选被解析器静默丢弃仍放行等反例。
- 主进程在同步后的 main 基线完成 353 项扩展回归：352 通过，1 条既有 UI 资产字符串断言失败，无新增失败。该断言仍要求旧 `findChartCandidate(targetCode, found)`，使用未修改的 `5ad058c` 资产和相同测试方法复现同一失败；本批不改 UI 或弱化该测试。证据：`parent-final-regression.json`、`parent-baseline-ui-failure.json`。
- 实现代理额外回归 424 项：423 通过，同一条既有断言失败。
- 实际当日报告固定回放：旧序列化后研究视图为 0 且发布校验失败；修复后为 26 且校验通过，仍标 partial。26 条研究记录逐字段不变，main/H4 正式视图逐字段不变。证据：`parent-fixed-report-comparison.json`。
- 独立研究身份复核：26 条全部是 `sources=[luojie]`、仅观察、`watch_only`，没有正式动作；没有把研究候选升级为正式推荐。
- `git diff --check` 与编译检查通过。上述测试均隔离执行，未写正式报告、数据库或账本，没有启动服务。

源代码合并和实际正式重跑回执单独核对。正式运行目录同步时必须在发布锁下验证现存产物与日志哈希，保留原始产物，仅在版本快进且产物逐字节不变后衔接续跑日志版本；再执行既有正式入口。不能把上述隔离验收写成当日报告已发布。
