# 热点地图排序与收纳 v1.1 实施记录

目标：用户明确要求《ChanlunStrategy_热点地图排序与收纳_追加任务包_v1.1》优化至上线。本批主规范为[追加任务原文](2026-09-17-market-hotspot-sort-v1.1.md)，只替代v1.0来源顺序与地图全局前30只收纳，其余保护保留。

基线：核对 origin/main 与生产克隆均为84457ec99c276b2114b163eed6098fd4d2ef37e2，在 `.worktrees/market-hotspot-sort-20260917` / `codex/market-hotspot-sort-20260917` 隔离实施。主进程保持配置，落地继续Sol/xhigh/default、Fast关闭，主进程独立审查发布。用户已授权按追加包实施到上线，不重复要求设计或每步确认。

影响：B06行业统计和缺值真实；B07只改变热点阅读顺序，不改变正式排序；B08复用现有详情/图表；B09/B10隔离、数据和上线证据。B01不补数，B02不改价基，B03不改评估覆盖，B04不改身份，B05不改时效。

实施步骤：
1. 在 `tests/test_market_hotspot_frontend.py` 增加S1—S8行为反例，先运行确认新要求在旧版失败；日期/身份/部分清单等既有反例保持。
2. 仅 `chanlun/report_assets/report-v2.js` 的热点辅助统计/排序、筛选/分页/渲染/状态和对应 `.hotspot-*` CSS实现新行为。复用现有模型与详情，排序只作用副本。
3. 使用9/16固定输入独立核对89只43组、前6组/28卡、3种排序、组内有效首封、缺值、跨题材去重和过滤统计。无策略变化，不做收益回测或生产重跑。
4. 主进程复核实际差异、定向及直接上下游回归，同步资源和HTML引用，保证原始JSON/候选序列/分数/ref不变。
5. 同步最新main后提交发布，实际1440/390页面操作排序/筛选/展开/详情；保存手机截图、读回资源、保留运行目录原脏改动、清理本任务资源。

## 实现与离线验收

唯一实现CLI的实际turn_context核对为gpt-5.6-sol/xhigh，显式service_tier=default，全部本机MCP服务禁用；与旧任务相同配置，不采用角色默认模型替代。包内8项SHA256校验和一致。源码改动局限于热点JS/CSS和既有热点测试；旧组内来源顺序断言按v1.1改为新顺序，重复同题材/跨题材和身份日期负例保留。

基线26项通过；新增/替代5项行为反例在旧版预期失败，实现后热点22项通过。主进程313项发布前回归、原5项独立边界反例及独立计算的3种模式×43组全量顺序/统计比对通过，89股/26命中和原输入不变。另测旧行日期、报价缺失、名称未关联不污染有效行业家数且记录继续可读。同步最新main后重跑热点及来源标签测试，结果见最终验收。

默认前6组为通信设备6、半导体5、化学制品5、元件4、包装印刷4、其他电子4，默认28卡；高度模式前三为电力6板、元件4板、IT服务Ⅱ3板；系统模式前三为半导体4只、包装印刷3只、其他电子3只。均为9/16 15:20:48快照的阅读统计，非实时排名或策略收益。

首次候选资产版本67cfc735cacb，JS ced7bfcb66f0eda319db4a8683a6ffa6590f7398021c54778f8ff61bbe590e1d，CSS 5de12504839a83b1d88c928946370c3162ae22c621ecbf5758fd463bbbdf8778。该次暂存证明90份JSON不变，15份HTML除资产版本引用外不变；尚未发布。独立审查随后发现S5未知关联被写为0、S7部分来源标题缺少“已取得”限定，主进程均复现并交唯一实现者最小修正，另补仅名称行业不得显示确定0的同类反例。首次绿测不作为这些边界通过的证据，最终修正版须重新锁定源码/资源并回归。S6实际界面、S9线上资源与手机截图待执行。包内旧截图不作为新行为验收。

## 最终候选核对

以上S5/S7问题已由同一实现者修正，均有先失败后通过的定向反例。清单不完整时分开已知命中与待关联，真0仍为0；部分来源显示已取得涨停N家，无法确认有效证券的组显示系统关联待核验。统计排序和89/43/26/63真实样本不变，无新增发布门。

主进程同步main后运行316项相关回归、5项旧边界与3模式×43组全部行业/股序的独立oracle，全部通过。最终资产75baece93310，JS 1048c71ace13e1ae958888e0cb46b16690c24f45e90a4df1fca24a598a7128c0，CSS 5de12504839a83b1d88c928946370c3162ae22c621ecbf5758fd463bbbdf8778；发布副本逐字节一致，90 JSON和15 HTML业务内容再次核对不变。源修改完成后再生成资源，审查时的中间版本差异已消除。实际发布、S6及S9证据待下段追加。

## 上线与S1—S9终验

产品提交24900c050bc0087c9fa6d0ce0bb4426014fcb02f已进入main，Pages运行35177295919 completed/success。实际运行克隆同步成功，原9/8页面删除及compare页本地内容保留；不启动服务、日报、通知或数据补齐。线上15 HTML＋JS/CSS共17资源逐字节匹配，普通无release查询参数主站也加载75baece93310、默认count排序28卡。使用的是9/16 15:20:48快照，未称9/17实时行情。

| 项 | 终验结果与证据 |
|---|---|
| S1 | 通过：线上89股43组，默认通信6／半导体5／化学5／元件4／包装4／其他电子4，共28卡；标题含完整家数。 |
| S2 | 通过：线上高度模式电力6、元件4、IT服务Ⅱ3居前；通信通鼎互联2板首位。缺连板/非法时间反例在VM通过。 |
| S3 | 通过：桌面和手机“更多行业”展开43组89个唯一证券；清单30→60→89，完整顺序与地图相同。当前真实最大组6只，额外合成8只行业通过实际绑定click回调从6→8，跨题材去重另有合同验证。 |
| S4 | 通过：非首屏603900可搜索；通信筛选和002281搜索仍保留涨停6家、最高2板、系统命中1只，并独立显示当前1/6；清空保留排序。 |
| S5 | 通过：系统模式前三半导体4只、包装3只、其他电子3只，不加总每股池数；过滤匹配26只仍保留市场总数。混合未知/全部未知/真实0修正反例通过。 |
| S6 | 通过：1440宽页面无溢出；390×844下scrollWidth375，7个快捷行业按钮＋更多选择，搜索标签横排；默认及展开后的双列149px股票卡可读。已保存实际新版本手机截图。 |
| S7 | 通过：报价缺失、连板缺失、名称未解析、日期冲突及部分快照在离线故障注入中保留有效记录；partial标已取得，未知不冒充0，无新日报门。线上原核心流程可用。 |
| S8 | 通过：线上605358进入XD立昂微原详情、双池标签、60根K线/成交量/MACD实际可见；关闭保留height排序与搜索。603900未入池只读详情正常。原主推仍300095（61分）、688150（71分），原序/不可执行状态不变，全部80只；90 JSON及工作台整体hash保持。 |
| S9 | 通过：产品源码、发布副本、线上资源一致；日期/版本核对、真实手机截图和主站操作完成。 |

本地交付证据在本实施树 `.cache/hotspot-sort/`：`live-resource-result.json`、`final-regression.log`、`parent-oracle-result.json`、`runtime-forward-result.json`；截图目录有 `desktop-default.png`、`mobile-default.png`、`mobile-cards.png`、`mobile-more.png`、`mobile-detail.png`。截图取自11点后实际新版本网页，不使用包内旧图。截图只用于本次验收，不覆盖生产HTML或报告。

用户原工作区30,541个冻结文件及HEAD未变，运行目录90份报告JSON未变，仍仅保留原两处本地改动。实现/修正/审查CLI均已退出，本机MCP服务在本批CLI禁用；进程复查未发现本工作树的遗留Codex/codegraph进程。浏览器临时视口已恢复，主站已回到默认排序并清除验收筛选。局部原因/K线/板块字段缺失按既有规则降级，未重新归类行业或改变策略。
