# 主要功能可用发布 Implementation Plan

> **历史范围说明（2026-09-14 规则整理）：** 本文是限定第七轮的计划与当时结果，原文和轮次保留。“首次产品验证后停止”等限制已在 [三个收口点继续实施](2026-09-14-three-closures-release.md) 的明确范围内被替代，不能用作后续所有任务的冻结门槛。长期组织和验收口径以根 `AGENTS.md` 为准；本次仅提交规则文档，不执行本文产品发布。

> 本计划沿用同一任务配置与原轮次；不启动新任务规避计数。代码交原已确认 GPT-5.6 Sol / xhigh、Fast 关闭的实现者，主进程独立验收。用户已明确回复“授权这个限定第 7 轮”，当前先准备完整输入/回归与独立审查，随后集中实施。

**Goal:** 已有股票数据正常显示，桌面和手机主要详情入口可用，局部缺失只影响对应模块；通过实际启用范围验收后按既有授权上线。

**Architecture:** 从实际发布基线 aa991b9 建立隔离候选，保留第六轮失败工作树和诊断包。复用已验证的 ref 定位与图表挂载；来源记录逐条呈现，不引入失败的自动归约。跨期计算未启用时明确说明，原始数据保留。

**Tech Stack:** 现有静态报告、JavaScript/CSS、Python 资源暂存脚本、离线 Playwright 路由验收；不启动本机 HTTP 服务。

## 授权与停止边界

2026-09-14 用户的新可用性标准替代旧完整性交付前提；不再要求复杂自动合并、全部历史比较或全部字段完整。原各 6/6 轮停止记录未改。用户已明确授权这个限定第 7 轮。沿用原计数，累计上限为 7；本轮首轮产品验证后若需要新的实质修正，按旧规则停止，不暗开第 8 轮。

## 已授权实施范围

1. **恢复取图与统一主要入口**
   - 文件：`chanlun/report_assets/report-v2.js`，必要的局部样式在对应 CSS。
   - 采用发布基线 `findRawCandidate(item.ref)` 与目标容器参数，按同一快照显式找真实原始记录；不采用第六轮要求原始池额外拥有 source_scope/strategy_results 的新过滤器。
   - 列表、比较已选股票、变更区的当前股票按钮都进入正确桌面详情或手机抽屉。
   - 历史记录继续只读；没有核验好的旧快照图表上下文时，保留旧记录与历史日期，并明确旧图暂不可用。不能用当前图表替代旧图，不能将这个状态描述为原报告数据不存在。
   - 不把第六轮大块 JS/事实模型整体搬回新候选。
2. **各来源独立展示并保留价格用途**
   - 在比较/详情/历史原始记录中保留策略版本、来源、不同计划和原数值。不能按同价删除 sv2，也不因合并失败吞掉 B 来源。
   - 本批不构造复杂自动合并的唯一事实；不足以核验为同一来源的记录并列展示。
   - 非正式或用途未核验的旧参考价保留“原记录参考/用途未核验”，不进止损、收益比或跨期计算。来源冲突可读，不选首项冒充一致。
3. **模块内缺失与计算降级**
   - 成交量、均线、市值、确认条件、历史请求各自处理空值/错误；有 OHLC 就继续画价格图。
   - 未声明条件不写“已满足”，空风险不写“无风险”；缺值不填零。
   - 不启用不可靠历史配对/差值/升级判断；可以并列看原记录。UI 直接说明“跨期计算暂不可用”，不以未知输出“未变化”。
4. **验证并发布同一个候选**
   - 新的 40 条映射见同目录 JSON。旧原期待和旧失败保留；“增强未启用”“局部缺失保护通过”“已启用功能通过”分别记录，不能全改成 PASS。
   - 真实 000636 9/11 的 50 根 K 线/末根 OHLC 必须通过；000636、002600、301682 主要入口和 600184 多来源保留；旧 8/14 完整集合仍可读。
   - 桌面 1440/1366、手机 390/320，实际按钮点击、关闭重开、快速切股/切日期、缩放/滚动。检查可见容器和对应 OHLC，不能只数 canvas。
   - 注入缺量/均线/市值/条件/历史错误，断言仅对应模块降级；同价 sv1/sv2、不同计划、19.80 旧价未知用途均不能丢。
   - 先暂存实际将发布的源 JS/CSS 和页面资产版本，仅使用 `scripts/stage_report_asset_version_updates.py` 等既有受限入口；若需要变更展示投影，必须独立核对原报告对象完全不变，不能顺便重跑报告。
   - 提交前同步最新 main 并再验相关回归，核对目标祖先。通过后沿用既有授权合并发布，回查 Pages、实际资源 hash 和桌面/手机入口；runtime 无活跃任务且干净时才更新。

## 保持不变

不改选股策略、正式名单/顺序、价位合同、库/账本/报告原始事实、通知；不补历史，不恢复 M5。保护 B02/B03/B04/B06/B08/B09/B10；B01/B05/B07 行为不动。原 dirty 主目录、第六轮失败分支、轮次台账和诊断 ZIP 不覆盖。

## 当前事实核对

2026-09-14 读取：待修分支、runtime 与远端 main 的 HEAD 仍为 aa991b9；runtime 干净，三个既有调度均 not running。第六轮原 24 个受跟踪脏文件仍在隔离树，没有新增产品修改。

## 第七轮执行结果追加

本轮已集中实施并首次验证，产品 JS 冻结于 `9736ae6eb7afc5631e03ee6445ee3ec41a809f59f6966142f6178930876e08a2`。独立验收发现同日异价基借图与未核验合同价格仍进入正式样式图表/详情。累计 7/7，停止产品修改，不合并、不发布，未暗开第八轮。完整裁定、旧要求与新范围区分及具体消费者位置见 [第七轮结果](2026-09-14-ui-availability-round7-result.md)。此前段落为执行前记录，保留原样。
