# 9月23日行情补发发布记录

## 目标与范围

用户要求“补上线”。本批按会话中已说明的行情补发范围交付：提供9月23日可核验收盘行情查询，明确板块资金排名与正式推荐仍待补。不是把不完整输入认定为完整正式选股日报，也不宣称自动取数故障已修复。

基线为实际运行仓库main `3d5e722463dd7658d3f0133fcea38d8d6e26e0a3`，实施分支 `codex/report-recovery-20260923`。仅生成静态补发产物与本记录，不改策略程序、发布门、正式行情库、账本或通知。

## 数据与页面

- 行情来源腾讯，交易日固定为2026-09-23，5450只，原库登记范围分母5878，覆盖92.72%。缺失428条分别保留，不填旧数据。
- 102个共同样本与同花顺问财的开、高、低、收、前收盘价一致。股/手换算后成交量差最大0.5手，成交额差最大0.5元，均在显示精度范围内。问财仅用于交叉核验；不是全量双源验证。
- 页面显示不复权原价，交易日与实际生成日分开。无参考线、策略评分、推荐或买卖动作。
- 首页与 `docs/2026-09-23/index.html` 提供行情补发入口；原访问口令、已验证浏览器状态和上一期正式日报入口保留。
- 补发数据单独保存于 `docs/recovery/2026-09-23.json`，类型 `market_data_recovery`，`is_official=false`、`formal_actions_allowed=false`。补发索引为 `docs/recovery/index.json`。
- 正式 `docs/data/index.json.latest` 仍为2026-09-22，准确表示最后一份完整正式日报；没有伪造 `docs/data/2026-09-23.json`。下一次正常日报仍使用原有正式流程。

## 边界

B01/B03：日期、身份、数据覆盖有明确计数；没有写正式库。B02：仅展示未复权行情，不混用策略价基。B04/B06/B07：补发不是正式推荐，按代码排列，不产生策略或资金排名。B05：不重标或复活历史信号。B08：盘中/盘后程序不改，24日盘中行情未混入。B09：独立补发类型，不触及账本、通知及正式校验。B10：实际输入、局部缺失、版本和发布证据分开记录。本次不替代原正式策略规则或任何既有价格保护。

## 验证

- 5450条身份唯一、收盘日期和OHLC/量额字段检查；首页、归档HTML与独立JSON逐字段相同。
- 102个配对样本结果与已归档交叉核验一致。
- 22日正式JSON/HTML、正式索引、原JS/CSS与基线字节一致。
- 10项收盘快照测试、5项正式发布来源保护测试通过。默认正式校验明确拒绝此补发类型；未放宽正式门。
- 正常历史资源版本更新保持补发归档字节不变。
- 1440px桌面、390px手机浏览器验收：访问门、错误口令拒绝、原登录状态、代码/名称搜索、交易所筛选、分页、空态通过，手机页面无横向溢出，宽表格可局部横滑，无浏览器脚本错误。
- 本地预览使用独立浏览器上下文和内存页面，无服务或监听端口；上下文已关闭。未操作用户浏览器的现有认证状态。

## 发布与回退

本地验收完成；远端提交、Pages发布和实际线上回查完成后，在交付回执中记录SHA与URL，不以本地通过冒充上线。

回退只撤本批首页/补发入口，不覆盖之后新生成的正式日报，不撤其他策略或业务数据。完整9月23日正式选股结果、板块取数恢复与长期备用源接入仍是后续事项。
