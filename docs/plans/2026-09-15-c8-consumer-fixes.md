# c8 上线复核后的下一批优化方案

状态：用户在本方案交付后明确设定目标“修复 & 上线”，本批已获实施与发布授权。以下诊断与范围保留，实际执行结果逐项追加。

基线为远端 main、运行目录和干净分析树共同的 `c8aa2e7c50fdc62dbf84af2ce15deb50ccd593af`。已直接读回线上 JS/CSS/9月14日 JSON，三个 Git blob 均与用户材料相同。用户根目录及运行目录比较页原改动保留。输入只有本次 Markdown；未冒称下载或验收其列出的全部附件。

## A：空均线误报与当前状态表达

目标：均线缺失不再被误报为数值冲突；当前使用提示与工作台是否可执行一致，原策略结论和分数继续可追溯。

- F1 已从实际页面复核：42 个实体、56 个来源记录中，13 个实体的20个来源含 MA5/MA10 invalid；星德胜的同名字段实际为展示用空数组，chart_ma 明确 unavailable。修正 `recommendation_evidence._project_daily_structure` 的数据类型边界，仅将顶层ma5/ma10已定义的展示序列排除出标量比较；空序列保留缺失，非空序列不凭末值冒充已核验当前指标。嵌套标量槽里的错误数组、非法标量、相互冲突的有效数值、价基/日期异常继续保留，不改变其他均线字段合同。
- F2 已用当前完整渲染函数复现：`is_executable=false` 同时输出“正式动作：可上车”和“偏执行优先”。列表和首屏以已有当前状态、具体阻碍为主；原动作/原理由标为“原策略结论/依据”，保留在来源追溯区。覆盖列表、图前判断、详情首屏及来源标题，检查它们直接关联的比较显示；不靠替换某一句文案掩盖同类输入。
- 已发布页面含内嵌证据，不能只改 Python 后等待下一份报告。限定首页和9月14日归档的展示派生字段，保留原始 JSON、inlineReportData、源 ref、有效分钟证据和已有各来源。当前通用 stage 会优先读取旧 workspace，并因序列化信息不足丢失部分已验证证据，不能原样使用。实施时仅更新受影响的日线证据、摘要、相关main_rise_clue旧误报及直接状态消费者，先证明同来源基线可重现，再复用现有叶子白名单替换工具生成可审查差异；保留原跨期记录，不建设全历史迁移器。

允许变化：工作台状态分组和统一列表显示顺序可按既有状态规则联动，逐项记录前后；正式选股名单、原策略动作、分数和正式排序不变。保留原发布证据的单因素敏感性检查中，星德胜由 evidence_blocked 变为 formal_incomplete，仍缺参考价和失效位、仍不可执行；原62分不变。它可能按现有规则进入“优先关注”，不等于 ready。这是待实现方案的影响证据，不是已修复或完整发布包验收结果。

验收：真实9月14日从来源证据到当前状态再到可见页面串联；空/非空序列、正常/非法标量及真实冲突正反例；缺价不放行、研究/事故身份、多个正式来源及零分；手机详情/图层/缩放/关闭重开和事实跳转保留。除允许的派生字段与资源版本外，页面原始数据不变；原来有的来源、K线及分钟证据不得丢失。

## B：罗姐独立研究入口与晚期修复脚本衔接

目标：专用研究入口与已核验统一清单保持同一批研究对象，同时保证下一次自然生成不会再次清空。

真实输入为 `luojie_pool.partial` 30只、统一清单30只、旧workspace专用视图0只。已追到 `daily_run.sh → repair_strategy_scorecard_snapshot.py`：后续修复先重建workspace，又将luojie按picks_pure共同全集校验并清空；随后只替换HTML内的inlineReportData，原decisionWorkbench留存30只。

拟修改两处必要边界：

1. 后续修复脚本沿用已批准的罗姐独立研究合同，仅对明确合法的独立research结果采用相应校验；正式main/H4及其他需要共同全集的视图保持原保护。验证完整、partial、空、失败、混合/未知身份与真实越界反例；不粗暴删除所有上游检查。
2. 既有9月14日JSON保持不变，罗姐专用入口窄范围复用同报告、同来源、已经核验的研究展示投影，并同步数量、partial提示、预算范围及详情ref；不从任意raw数组补入、不转正式。若现有投影不满足身份/日期/来源校验，局部说明缺口。

验收覆盖真实生产后处理的纯函数链、保存/读回后的HTML数据选择及实际专用入口；核对原始30、统一30、专用30的代码集合/来源身份，而非只检查三个数字。原始数据写回、账本修复和生产补跑不在本批范围。

## C：评分正确性单列研究

只形成有限固定输入对照，不随A/B改变正式评分。

S1 实际把未声明回踩时的任意非空确认文字当作回踩并加15；S2 实际将原始金额sector_flow与0.6比较。另有研究质量分与观察alpha消费同一金额字段，不能全局更改单位或临时除以亿元。

先分别研究回踩fallback、资金强度fallback及组合，保留真正回踩、通用分钟形态、前8名/明确热点、缺失与异常输入。逐样本先复现旧策略原分、各项贡献和身份，复现不了的列缺样本。已发现直接重评序列化星德胜会因丢失顶部量比得到47；按原适配器显式恢复已保存2.88后才复现62。因此62→47既不能当生产新结果，也不能当收益改善。

已有三份报告中decision-v2记录集中在9月14日，来源重复和未入选背景不足，不构成成组收益验证。后续只使用已有可核验输入，按下一可成交时点及实际覆盖范围形成行为/收益对照；缺数据如实保留，不追加无限历史补数。正式策略身份和分数变化须在结果确认后另行落地。

## 执行与保护

建议顺序A、B两个小补丁各自验证，C独立研究。主进程保持当前配置，拟由 GPT-5.6 Sol / xhigh、Fast关闭实施；这是本次待确认配置，不将旧任务配置自动视为新授权。主进程负责关键复现、实际差异、数据保护及最终验收。

本次涉及B02/B04/B06/B08/B09/B10；B03涉及研究显示覆盖，B07在A/B仅保留评分/排序、C才研究变化。B01不补数，B05不改变事件时效。确认后从当时最新main建立新实施树；不改原脏树，不启动生产服务，不重跑日报、不通知，不使用尚未获准的密钥URL登录操作。先交付本地可审查结果，发布按当时明确授权办理。

证据目录：当前文件所在目录。核心证据为 `current-public-baseline.json`、`parent-counts.json`、`f2-results.json`、`f1/`、`f3/`、`scoring/review.md`。本方案保留“小步快跑、缺哪项降级哪项”的规则，不把缺少非必要历史能力作为A/B交付前提。

## 2026-09-15 实施授权与基线

用户“修复 & 上线”承接本方案：A/B完成修复、独立验收、合并与发布；C按既定范围做隔离对照，不据此修改正式评分。主进程保持当前配置，产品实施进程使用GPT-5.6 Sol/xhigh，service_tier=default、Fast关闭；首次启动必须核对真实回执，不嵌套派发其他型号。

最新main仍为c8aa2e7c，从该提交建立 `.worktrees/c8-consumer-fixes-20260915` / `codex/c8-consumer-fixes-20260915`。原用户工作区、上批成果树和production-runtime保持隔离。当前实施树AGENTS blob为cb2413af0a3cd70b972c762f41e2ee7b56eaea7e，引用方案受跟踪；根规则的集合消费者补充继续生效。代码实现分为互不重叠文件的证据/后处理与前端两路，主进程独立审查和验证。

实施证据目录 `/private/tmp/chanlun-c8-consumer-fixes-20260915/`；诊断证据原目录保持。若自然调度产生新报告或main前进，提交发布前重新核对并保留新业务数据，不能以9/14首页覆盖更新的日报。发布时只更新已核验当前派生字段及相关资产；不重跑生产、不发通知、不写正式库/账本/原JSON，不使用前轮未获准的密钥URL传输。

### 初始执行与C研究结果

主进程在新树运行6个直接相关模块，基线104项通过。两个产品进程的实际启动回执均为gpt-5.6-sol/xhigh，命令明确service_tier=default，子进程保留workspace-write沙箱；backend进程13716/13726、frontend进程13871/13881已登记，结束后另核对退出。主进程正在独立准备验收，尚未把子进程工作记为完成。

C已完成现有材料范围内的隔离行为对照：三份报告中仅9/14保存decision-v2，共54条来源行；先复现旧决策字典后，基础/融合6条可比，观察/罗姐48条不能复现的记录保留为缺样本，未纳入变化统计。基础4条与融合2条的评估器recommend计数原来各1，单独关闭通用确认回踩fallback、单独关闭原金额0.6 fallback及组合均各0；这不是新正式名单或收益改善。星德胜隔离分数62/47/42/27只说明分类边界敏感性，原生产分数与策略不改。5个明确正反例通过，3份原JSON的SHA256不变。详细边界、逐条对照及排除原因见实施证据 `research/result.md`、`research/counterfactual-results.json`；没有补数或生产执行，不将本结果解释为评分正式发布授权。

### 自然调度并发产物的发布保护

15:20:06自然补跑启动，日志最后写于15:24:07.726；任务已经退出、退出码1。失败发生在生成后的罗姐15分钟输入校验：池为unavailable、候选0，日志明确保留本地产物但不提交。本任务未启动、重跑或修复这次自然任务。读取时origin/main及runtime HEAD仍为c8aa2e7c，暂存区为空，9/15 HTML/JSON仍未跟踪，未随main发布。

因此发布保护更新为完整保留runtime的8项工作区变化：9/8 HTML删除、原compare修改、data.json/两个索引/首页修改、9/15 HTML及JSON新增。公开站仍以已验收的9/14为本批页面基线；runtime保留未发布的9/15产物，不能将其覆盖为9/14，也不能借本次代码修复将校验失败的新日报推到main。只允许同步其中HTML的资产版本属性，全部原JSON字节及删除/未跟踪状态保留；切换先在临时克隆演练，锁内重新核对。此条替代本批最初“runtime仅compare脏改动”的状态假设，不扩大数据、补跑或通知授权。

### 实现与独立验收记录

A/B产品代码已完成：顶层MA5/MA10展示序列退出标量比较；当前不可执行提示贯穿列表、详情、图前和比较，原结论明确标为来源追溯；合法Luojie独立研究结果通过专用入口及晚期修复链保留。预算/健康矛盾、额外同来源冲突、错误证据路由、保留日线标量/日期漂移及内外层事故身份均有明确反例，独立复现后修正，不删除原保护。

两页实际暂存保持42个实体、56条来源和原ref/顺序，纠正20条来源、13个实体的MA误报；其中12个实体的当前派生字段随既有规则变化，601360的主来源未变。星德胜为formal_incomplete、62分、不可执行，仍缺参考价和失效位；原有“结构破坏”也按同来源旧证据恢复可见。Luojie30、highlights10及全部原分钟、价格、风险、跨期记录保留，无新增正式候选或执行动作。

主进程独立页保护每页345项通过，24项完整消费者回放通过；额外标量、日期、来源及事故反例闭环。可允许的主升线索变化逐项核对：20条旧翻译均可按同来源完整重现，新标签只揭示既有事实，不产生新策略信号。资产版本e0d1f97ede32，JS SHA256为444ce527208e2294b907ddde5d575597038598771d6cb6040dbfeef5b9f2690c；两页仅修改两个派生值和资产属性，其余12个页面仅资产属性变化，88份发布基线JSON不变。

产品定向回归及审查已通过。打包后发现暂存/F2测试依赖可变首页，已改为固定的c8历史压缩夹具，保留旧错误输入与全部断言；不放宽产品对已修正输入的拒绝条件。夹具解压与原冻结首页字节完全相同，由203466字节压缩文件和来源hash验证，归档相对路径可精确重建。最新main同步后，主进程在最终发布包及固定夹具上132项相关回归全部通过；打包后106项其他页面/JSON/源码资产保护通过。已知事件风险文字旧用例与不可靠findChartCandidate兜底旧断言保留为基线问题，未为它们恢复错误借图。本地file URL预览被浏览器策略拒绝，未绕行；实际桌面/手机操作放到公开首页发布后验收，归档的登录态不冒称已验收。当前尚未推送或切换runtime，后续发布结果单独追加。

### 首发回查及展开审计补正

上述发布前状态随后被实际执行替代：8bef245f9f652fe307387e59fb5efd0008e4d396已推main，runtime同版本；Pages运行34946983247成功，18份公网文件逐字节与提交一致。运行切换在发布锁内进行并先用临时克隆演练，89份runtime JSON、8项未发布/用户工作区变化全部保留；公开日报继续9/14，runtime未发布日报继续9/15。未重跑、通知或写库。

真实浏览器1440×1000确认星德胜62分、当前不可执行、两项价格缺口、均线缺失空态及原80条30分钟证据可读；K线和60根范围正常。罗姐专用入口30只、20→30加载、partial60/100说明正常。390×844实际打开详情、K线与MACD可见、切换结构/60根、关闭及重开正常。

展开罗姐002202完整审计时，主进程发现01A仍把原研究记录64显示为决策分；因此首发不记为完整验收通过，继续同范围补正。根因是渲染器已收到watch_only，但总分/分项仅检查事故标记。补正以明确formal且非事故为数字显示条件，研究/事故保留排序和原始审计记录；正式62与0保留。真实64专用和统一来源审计、正式62/0及事故5个完整渲染对照均通过，输入未变；最新main同步后64项受影响回归通过。补正资产33374da3d82e、JS SHA256 a340ca256680326534e41e8a903397e6535aa4a2d3d91551f478ac2363b018b0，仅14个页面资产属性变化，全部两页派生值与88份JSON继续不变。补正公开展开验收另附回执。
