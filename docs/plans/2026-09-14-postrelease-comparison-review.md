# a0edd61 上线后比较文案修复

## 本批范围与基线

用户要求按下载中的 `ChanlunStrategy_上线后Review_a0edd61_证据包.zip` 和项目规则落地处理。本批仅处理 Review F1/F2：比较中的参考价用途标记、研究观察对象的条件文案。沿用展示任务的主进程当前配置、落地子 agent `gpt-5.6-sol / xhigh`，不启用 Fast；主进程独立核验实际差异、回归和发布结果。

2026-09-14 核实远端 main 与实际运行目录均为 `a0edd61db111ca47eab59659a75a11fe0389bca0`。隔离实施分支 `codex/postrelease-review-a0edd61-20260914` 从最新 origin/main 建立。根工作区保留旧分支及用户改动；运行目录已有 9 月 14 日报告相关变化，本批不覆盖、提交或重算这些报告。根与实施树 AGENTS.md SHA256 均为 `3dccacd0290ef3c49c0ea8921a31240d8a3e86c1aa785ab202c9f7f4b74785a4`；根规则与引用方案在实施树受 Git 跟踪。

## 根因与最小改动

- F1：`comparisonStrategySource` 直接拼接合同价格，未携带现有用途状态。复用已有选定合同的用途判断，为未核验参考值补原始记录说明，覆盖轻量关键价位、身份与来源动作和完整比较的直接消费者。保留已核验价格及缺值分项，价格数值和计算路径不变。
- F2：`quickComparisonDimensionValues` 仅凭 `is_executable === false` 输出“正式条件尚未完整”。改为依已有明确身份给文案，研究观察与身份未声明保持区别；已有具体阻碍优先原样展示，不以核验文本是否存在推断满足或失败。

保留本来源 K 线、正常详情入口、手机关闭重开及比较移除。复杂历史比较、跨来源借图、自动合并、缺量能与手机视觉整理不在本批增强范围。不得改策略、正式身份或排序、数据合同、正式库、账本、自选与通知，不补历史，不重跑生产。

## 影响边界与验收

本批影响 B02 价格用途显示、B04 身份文案、B06 已有事实与缺失语义、B08 比较直接消费者一致性、B09 发布与数据隔离、B10 固定输入和演进证据。B01 数据自愈、B03 评估覆盖、B05 生命周期、B07 评分排序不改行为。不改变选股行为，因此执行展示和语义回归，不要求收益回测。

1. 固定 a0edd61 源码与 9 月 11 日原始报告，先复现 F1/F2；验证原始对象不变。
2. 未核验合同参考 10 在统一详情、轻量与完整比较显示用途说明且不画参考线；已核验合同、缺值、合同别名和有明确阻碍的输入保留原语义。
3. 000636/002600/301682 研究文案正确；原有下一核验和取消条件不丢。未知身份不冒充正式待确认。
4. 定向回归与直接上下游测试，桌面和手机实际点击比较、查看当前图表、关闭重开和移除；保留四股本来源 OHLC 正例及异价基不借图负例。
5. 发布仅更新源/发布 JS 和必要资产版本；HTML 除版本外一致、报告 JSON/CSS 不变。提交前重新同步 main 并重跑相关回归。运行目录合入前核对自然任务状态与脏文件，存在报告交叉时仅采用有证据的最小隔离更新。
6. 线上资源字节、版本和桌面/手机可见结果分别读回。本机不启动服务或监听端口，有限离线浏览器测试在结束后核验任务 PID 清理。

## 当前证据

证据包已只读展开到 `/private/tmp/chanlun-postrelease-review-a0edd61`。主进程确认线上三只真实研究候选均复现 F2；F1 为受控未来载荷情形，不声称当前真实快照已含该合同值。隔离基线 47 项相关测试通过。后续结果追加于本节，不把计划当作完成或上线证明。

主进程独立验证：201 项相关回归通过；20 项语义检查覆盖三种合同入口及 verified/formal/unverified/conflict/not_applicable/缺失用途，核对统一详情和完整比较直接消费者，未核验参考 10 仍不得进入计算。新旧固定输入均保留。最终源 JS 与发布 JS SHA256 为 `f1d383f87c26019dfccd8e4e103a34863d491f2fe18c6e214952e08ecfef6e96`，资产版本 `b686cb85deea`；14 个 HTML 入口仅更新资产版本，报告 JSON 和 CSS 不变。

扩展测试定性纠正：实施子 agent 曾报告更大集合有 4 项失败，但原输出不完整，不能据此认定旧问题。主进程在干净 `a0edd61` 和新补丁运行完全相同的 4 个扩展测试模块，各执行 202 项，均为 200 通过、相同 2 项失败、0 错误/跳过。失败为 `test_main_rise_guards_and_event_risks_are_visible_and_safe`（结构化事件风险文本合同）及 `test_v2_asset_uses_correct_raw_pools_and_kline_order`（仍要求已停用的借图调用字面串）。本批未修改这些断言或相应产品路径，作为范围外待办保留，不把扩展测试称为全通过；新旧证据为 `old-extended-regression.*` / `new-extended-regression.*`。

独立浏览器同脚本对照：旧版 29 项中 24 通过、5 项预期失败（F1、三个尺寸 F2、未知身份）；新版 29/29 通过，无脚本错误或写请求。覆盖 1440/390/320 尺寸、四股本来源 OHLC、比较/变化看图入口、手机关闭重开、移除重选、用途未核验价不画线和异价基不借图。截图和完整结果位于本地证据目录 `qa/new-f1f2/`，浏览器测试无服务监听，runner/Chrome 清理已复查。另一独立子 agent 审查本批 17 行源 JS 差异，无可复现阻断。

运行目录交付边界：9 月 14 日报告因既有正式质量校验未通过而保留未提交产物，本批不替其发布或重跑。发布合入与这些产物的 HTML 资产版本存在重叠，需先在隔离副本演练：保留原始 HTML/JSON/删除/新增状态，仅刷新 HTML 资产版本后搬运本地差异；无同输入保全证明则暂缓运行目录同步，不强制覆盖。
