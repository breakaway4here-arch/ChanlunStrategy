# 2026-09-11 选股正确性修复：验收与发布决定

主进程已批准本批发布。依据是固定输入下的事实纠错、真实正负例和独立回归通过；现有样本不足以证明收益提升。本次不扩大正式推荐范围，不继续扩量补历史数据。

用户授权由当前主进程负责方案和独立验收，GPT-5.6 Sol / medium 负责实现；本任务内沿用该配置，审核通过直接上线。长期规则不固定旧型号。总方案及后续防回退要求见 [优化方案第12—14节](2026-09-10-preclose-selection-quality-repair-plan.md)。

## 发布范围

| 边界 | 修复及验证 | 限制 |
| --- | --- | --- |
| B01/B03 | 分钟过期、根数不足、混合来源失败及异常类型保留到内部诊断；失败不冒充无确认 | 不增加请求，不改变来源顺序、异常回退或可用判定；历史non-final自动修复未在本批完成 |
| B02 | 保留既有价基和截止保护，实际相关回归通过 | 不改Sina qfq标签，不引入仿射变换；证券/指数号码冲突问题继续保留 |
| B04 | 原策略身份、买点白名单、风险阈值和恢复组合保持 | 右侧观察没有混入原加速入口；新增研究召回不转正式 |
| B05 | 预跑在评分及三池派生前执行既有十日门；缺失/非法源坐标和年龄不伪装成新事件 | 保留原best-only策略，多买点重选和全历史日历契约留待后续 |
| B06 | 真正EMA5近期穿越且当前站上；阳线实体方向正确；分钟买点/背驰/分型使用同频新鲜坐标 | 不额外收紧恢复组合；独立MACD、缩量等质量分支保持 |
| B07 | 共享市场上下文优先；融合准入strong/weak不再冒充市场阶段 | 原市场阶段算法与分数阈值保持；市场模型有效性未重新校准 |
| B08/B09 | 预跑与正式决策适配器同输入结果一致；正式final门、只读隔离、公开白名单和幂等回归通过 | 未手动执行完整日报或额外推送；部署后自然业务产物需另看实际source_sha |
| B10 | 独立发布工作树、固定输入hash、负例、源码审查、完整回归及发布后复核 | 不保证以后绝无bug；后续改动须保留负例并运行相关回归 |

代码涉及9个模块及9个对应测试文件：市场决策、融合准入、预跑数据/运行/编排、信号时效、强启动、分钟确认和趋势延续。`run.py`、调度配置、正式行情库、账本及既有日报产物不在改动文件中。

## 固定输入对照

主进程在真实生产基线和待发布源码分别调用纯预跑入口。使用原9个冻结盘中输入，加上原任务已保存的9月10日、11日输入，共11日；未请求额外历史行情。原基线完整三池与既有快照一致。

| 日期 | 原主池 | 修复后主池 |
| --- | ---: | ---: |
| 08-28 | 0 | 0 |
| 08-31 | 1 | 1 |
| 09-01 | 2 | 2 |
| 09-02 | 0 | 0 |
| 09-03 | 0 | 0 |
| 09-04 | 0 | 0 |
| 09-07 | 0 | 1 |
| 09-08 | 0 | 1 |
| 09-09 | 27 | 0 |
| 09-10 | 0 | 1 |
| 09-11 | 0 | 0 |

H4与加速池在11日两版本均为0。新增为9月7日高澜股份、9月8日利扬芯片、9月10日大港股份；不是额外扩大候选入口，而是纠正共享市场事实后原候选通过现行决策。9月9日减少由市场口径、真实确认和十日门共同导致，不能将27→0等同于收益优化或全市场没有机会。

9月10日大港股份使用同一候选、同一上下文：预跑实际决策与从`run.py`原样提取的正式决策适配器结果一致。旧口径总分48/观察，修复后63/推荐，市场阶段由弱市纠正为震荡；这项检查没有执行正式应用启动、完整盘后取数或发布。

最终补入的分钟事件时效及EMA质量消费者保护，另以只在内存恢复旧分支的对照运行11日，完整三池与前一版一致。现有11日没有证明这些边界会改变名单，新增真实负例证明旧错误可复现、修复后拒绝，同时合法正例与独立质量路径仍通过。

扩大观察的24个成熟日结果仍为：新增组T+3/T+5日期等权观察值0.17%/0.58%，原有共同组0.49%/0.94%，合格未入选背景0.82%/1.11%。这是研究观察，未证明可交易收益优势，因此不扩大正式主推。

## 独立验收证据

- 全仓实际运行：**1987项，0失败、0错误、0跳过，退出0**。源码在测试过程中未变；一次网络尝试被拦截，实际无外部请求；无越界数据库连接、残留测试子进程或后台线程。
- 提交前再次fetch目标分支，`HEAD=origin/main=4a8745651414dd8beb335deedbb8464751cff364`；同步后相关**232项通过，退出0**。第一次定向命令误写测试模块名，产生1个加载错误；修正为实际`test_preclose_formal_isolation`后通过，原错误日志保留，不算代码故障或通过记录。
- 11日最终实际回放23.09秒；内存旧分支对照23.03秒。网络与SQLite连接在模块导入前禁用，均无请求/连接，未导入正式`run.py`启动入口。
- 主进程实证旧分钟运行模块在新诊断负例上3项失败；新模块通过。代码与规格审查发现的元数据污染、非法源坐标、缺年龄默认0、旧确认及EMA质量遗漏均有负例并已复核关闭。
- 2026-09-11 16:00北京时间，主进程实际读取3个launchd任务均为`not running`、上次退出0，未见相关运行进程。生产仓库当时干净；本批可以在已结束的交易日任务后发布，不重跑今日报告。
- 主目录26个已有用户路径指纹核验无变化（目录项只核验存在性），不把主目录旧源码复制到生产目录。

本机完整证据目录：`research/selection-audit-20260910/phase2/release-validation-20260911/`（研究原始材料不随代码批量上传）。主要文件为`parent-full-regression-final-v2.json/.log`、`parent-post-fetch-regression-v2.json/.log`、`parent-replay-v1/baseline/`、`parent-replay-final-v2/`、`parent-market-entry-*-20260910.json`和`parent-minute-diagnostics-old-negative.json/.log`。每项含输入、实际源码hash与检查范围。最终部署回读另外保存为`DEPLOYMENT-VERIFICATION.json`，不能用本验收批准替代实际部署结果。

## 发布与回退

实际运行目录是`.worktrees/production-runtime`，独立发布分支为`codex/selection-quality-release-20260911`。在最新origin/main上提交本批源码/测试与规则文档，检查提交清单后快进发布。上线后回读生产HEAD、全部修改源码hash、报告/数据库hash及真实纯入口输出；既有报告、账本、封存和发送状态保留。

本批不改Worker/Pages字段契约，无需另部署Worker或重装调度。下次自然任务使用新代码；今日旧报告不人为改写，手机新结果送达不属于本次已验证事实。

出现异常时停止新推荐流入，优先修复并重新验收。需要源码回退时，在当时最新main建立前向提交，只恢复本批9个源码及对应测试路径，保留治理文档、报告、数据库及发送状态；不得`reset --hard`、整目录覆盖或把已知旧错误重新作为可推荐版本启用。可暂时关闭预跑对外候选，独立保留正式日报与审计记录；具体运营开关按现有runbook操作。

## 已停止或保留的工作

停止扩量补历史数据，缺口台账保留。未在本批完成的包括：历史non-final真实补齐后同次复评、全库历史写回、完整证券身份路由、跨日价格尺度映射、多买点重选/日期契约、新事件召回与跨日观察生命周期、完整可交易收益回测、自然调度后的新报告与手机接收。后续按预算和证据再安排；这些不计为本次已修或本次上线阻断。
