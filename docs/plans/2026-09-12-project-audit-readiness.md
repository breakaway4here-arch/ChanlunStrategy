# 项目审查修复：本地验收与剩余上线条件

截至2026-09-12，**R01—R10 已完成发布准备验收**：整合后的2198项测试全部通过，正式行情库覆盖为5365/5831=92.01%，剩余57822项数据修复已在当前库快照完成联合应用、实际消费者与回退验证。精确正式写入工具及10项授权测试已纳入候选代码，安全回归入口已保存。当前可以进入受控发布执行；尚未执行剩余正式数据写入、代码提交、部署或运行切换。发布执行范围与恢复步骤见 [发布执行说明](2026-09-12-project-audit-release-runbook.md)。

## 实现范围

| 项目 | 已合入隔离版本的行为 | 验收边界 |
| --- | --- | --- |
| R01 证券身份 | 股票／指数／交易所明确区分；身份待核验始终保留在完整分母；达标的单股缺口隔离，不再无条件整批停机 | 11/12 部分覆盖保留健康股票；晚期全市场与个人观察池注入不能带回待核验身份；当前正式覆盖92.01%，原89.14%负例保留 |
| R02 趋势语义 | 统一上涨／上升等名称；个股缺结构时不借大盘状态加分 | 固定输入及正负例通过；不宣称收益改善 |
| R03 板块排序 | 使用规范排名与来源，股票输入顺序不改变板块分 | 双入口及冲突／重复排名边界通过 |
| R04 回测成熟度 | T+1、T+3、T+5 各自满足终局行情和连续交易日期，未成熟收益不冒充完整样本 | 直接函数及研究脚本消费者回归通过 |
| R05 数量来源 | 股／手、成交额币种与来源逐根传递；按实际5／6／10／13／20／21根窗口核验 | 未知量不当作低流动性或0；量价估算明确标注；未知图柱留空，价格图保留 |
| R06 独立研究 | 罗姐使用独立15分钟研究预算，正式30分钟输入优先 | 完整与部分研究输入的实际 main 回归通过，研究不转正式 |
| R07 覆盖披露 | 页面展示检索、分钟、历史缺口，以及收盘身份和量能覆盖 | 真实 Python 投影到 JS 和手机页面通过；未知计数不补零 |
| R08 观察条件 | 锚点携带来源、日期、用途与有效性；缺锚点明确待证据 | 参考价不自动成为失效位，不复活过期事件 |
| R09 跨期对照 | 当期运行清单进入最终报告；策略身份稳定，数值可比性按单只股票的真实价基判断 | 旧报告缺价基保留成员变化，不强算数值变化 |
| R10 手机摘要 | 首屏展示结论和覆盖，保留展开的大盘证据与跳转 | 390、320、1440宽及键盘跳转通过；320宽新增覆盖说明无横向溢出 |

额外闭合的发布边界：正式策略项、账本资格和 shadow 发布资格统一消费最终健康状态；研究／观察的数量故障不污染正式要求。11/11与10/11的数量覆盖正例保留正式贡献，9/11时正式贡献为0，账本与shadow发布均不放行。

## v8代码验收证据（本轮数据补齐前的记录）

- 目标分支已重新同步，隔离分支 `codex/project-audit-fixes-20260911` 的 HEAD 与 `origin/main` 同为 `0e5225ea8351b7edc635368dbcb0aa39b0fd8586`。本轮修改尚未提交、推送或发布。
- 主进程最终组合测试 **2188项，0失败、0错误、0跳过**，包含显式开启的11日冻结回放。测试隔离层阻断18次网络／监听尝试，323次数据库连接均限于临时路径，没有正式数据库连接、测试期间源码变化或残留Python线程。见 `parent-final-combined-suite-v8.json`。
- 11份原始输入的字节hash与冻结记录全部一致。原始输入缺数量来源时如实报告数据缺口，不变成“健康空池”。见 `parent-frozen-eleven-input-hashes-v7.json`。
- 同一3481只真实冻结输入，数量展示修复前后的五类直接池及记录的量能／流动性特征完全相同。此项是消费者行为验证，不是实盘收益证明。见 `parent-ui-quantity-consumer-diff-v2.json`。
- 严格页面暂存检查通过；home/archive的inlineReportData与原9/11 JSON相同。14个HTML只更新资源版本，EOF格式及内嵌事实保留。最终asset version为 `406bebd4c9de`，JS SHA为 `6903a2a4f1381137a82fcb2f9e4f08ce4897a963824d094e6b54cf254d2ce9ce`。
- 本机真实Chrome与ECharts验收通过，全部页面请求由本地资源响应。身份／量能覆盖的合成展示样本明确标注；部分／全缺量图表保留OHLC与日期。页面错误0、监听端口0，浏览器已关闭。见 `parent-ui-browser-v8.json`。
- 主进程复查221个已记录任务PID，全部退出；没有运行中的本任务临时agent或私有目录进程。见 `parent-final-resource-check-v8.json`。
- 14个正式数据库／账本／日报等保护文件、25个原有用户文件均未改变。实际运行目录仍clean、仍为0e5225ea。冻结库SHA仍为 `ac5e55b7db175e747ba255c92b27f9eb2d44507330a8847a321f555a4178408a`。见 `parent-final-protected-state-v8.json`。
- 57900项联合数据候选仅在临时副本完成应用、逐行比对、重复执行、冲突中止与回退；未写正式库。迁移脚本与身份模块仍和主进程演练版本同hash；Worker源码也与此前类型检查和8项适配测试版本一致。正式迁移、实际Worker部署及外部接收仍未执行。

上述证据统一保存在本机 `/Users/yangfan/yf_source/ChanlunStrategy/research/project-audit-fixes-20260911/`。历史中间失败和较早报告保留，最终记录明确其替代关系。

## 发布执行事项（发布前验证已完成）

1. 覆盖不足已解除：本轮206个身份及完整窗口正式应用后达到92.01%，余下93个待核验旧身份继续保留在完整分母。
2. 旧联合修复包已有78项正式应用；剩余57822项已经完成当前基线的应用、幂等、全范围对照和精确回退预演，并形成固定正式候选清单。正式应用须按该清单另行获得数据写入授权，不能扩大到未核验历史数据。
3. 正式库实际读取及新数据消费者已验证；发布执行时仍需切换运行版本并回查页面、日报及实际接收。当前旧版本把92开头股票路由为SZ，不能将其与已迁移BJ身份组合视为已验收运行版本，也不能在回退到旧源码后继续正式输出。现有结果不证明策略收益提高。

## 2026-09-12 当前基线最终发布准备验收

- 重新fetch后的origin/main仍为0e5225ea，与候选基线一致。新增正式适配器的实际源码逐字节等于此前已验收版本；原4项测试的类AST完全不变，新增10项测试先在旧版出现8项预期错误，再在新版全部通过。
- 主进程使用已持久保存的安全runner，对最终代码重跑**2198项，0失败、0错误、0跳过**；18次网络/监听尝试被拦截，346次SQLite连接全部限于临时路径，无源码漂移或残留线程。见`release-readiness-20260912/current-combined-suite-v2.json`。
- 剩余57822项与补齐后的实际正式库逐项旧值一致；当前快照应用、重复执行、范围外全行对照、其他原有表一致及精确回退通过，正式库未变。当前清单只读预检为57822项计划操作、0项已经应用，保留严格旧值和路径保护。
- 实际资格名单3481→3481，三种检索排序均完全一致。输入中的8022个成交量差异全部对应来源已核验的修复行；只有002155一个量比特征产生约4.8e-8的差异，检索分数不变。没有据此声称交易收益改善。
- 预跑Worker实际处理方法和本地SQL适配的8项验证、TypeScript类型检查通过。页面源资产与此前实际桌面/手机验收一致，历史日报事实没有重写。
- 安全runner、数据预演脚本、消费者对照、工具集成RED/GREEN与独立审查结果已保存到`research/project-audit-fixes-20260911/release-readiness-20260912/`；新的候选清单替代v8过时的NO_GO包装状态，原记录保留。

历史授权演进：最初停止扩量；随后用户批准10只／40次研究试点；之后明确要求把不足的覆盖补到92%再验证。前两轮记录保留，当前状态以方案14.5及本文最新完成记录为准。

旧试点清单：`research/project-audit-fixes-20260911/diagnosis-data/bj-ten-identity-pilot-proposal-v1.md`。本轮结果：`research/project-audit-fixes-20260911/coverage92-20260912/validation-result-v1.md`。

本轮正式数据验收：206只、27788条新日线与21928条旧日线归档逐行核对；实际正式库覆盖函数及量额消费通过；155项相关测试通过。实际资格名单3481→3481，未改变现有资格参数。范围外日线／分钟原值、其他原有业务表及用户文件未变，临时验证数据库已删除，已记录任务进程已退出。代码和页面尚未发布。

## 模型与速度

所有后续实施agent显式使用 GPT-5.6 Sol / medium / service_tier default，Fast关闭；主进程核对了启动信息与命令。早先通用角色覆盖模型的事实已在方案14.3勘误，未将请求配置当作实际执行证明。当前无活动子agent。

## 2026-09-12 早期10只试点完成（历史记录）

用户批准的10只／最多40次请求已完成，共21次请求，无扩量、回退或限流。10只各30个交易日的原始／前复权来源完成独立核对；88条旧库重叠中83条一致，4条旧非最终记录更新，1条旧最终记录的量额差异已由第二来源交叉确认。300条研究候选经临时库、实际repository及20日流动性消费者通过，缺来源与60根历史不足负例仍阻断；主进程新增17项工具测试、3项消费者测试通过。

当时只验证批准窗口，958条窗外旧行和2只无同日重叠的旧历史未验证；正式库尚未写入，覆盖为89.14%，10只全数补回的上界仅89.32%。该试点的14个正式保护文件、25个用户原文件及冻结库均未变化，临时资源已释放。历史证据见 `/Users/yangfan/yf_source/ChanlunStrategy/research/project-audit-fixes-20260911/bj-pilot-20260912/validation-result-v1.md`。其范围与等待状态随后由用户明确批准的92%补齐任务替代；当前正式覆盖和应用状态以本文开头及方案14.5完成记录为准。
