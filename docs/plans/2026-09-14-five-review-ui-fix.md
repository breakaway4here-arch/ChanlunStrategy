# 五项复审后的两个展示修复批次

## 本批授权与基线

用户在核验和最小方案交付后明确确认“确认，落地”，授权实施 A/B 两批：A 为桌面图表头、正式评分可见性和导航解释；B 为事实按钮按对应来源跳转。主进程保持当前配置并负责独立验收；产品实施子 agent 使用 GPT-5.6 Sol / xhigh、Fast 关闭，实际启动配置另行核对。范围内必要修正与测试无需逐步再问。

2026-09-14 实施起点远端 main 和实际 production-runtime 为 `0a9d52f8908e6f0f231c874df4140507a444d9c7`。从最新 main 新建 `.worktrees/five-review-ui-20260914`，分支 `codex/five-review-ui-20260914`。用户主工作区旧 main 及既有改动保留；运行目录原有 `docs/compare/index.html` 改动保留。

根、实施树规则及选股计划第12—14节适用边界已经核对。实施树 AGENTS.md git blob 为 `cb2413af0a3cd70b972c762f41e2ee7b56eaea7e`，包含集合关联条款；规则和三个收口点、上线后比较修复、集合口径实施记录均受跟踪。不引入历史失败工作树的额外停止或完整性要求。

材料为下载中的集合专项证据包及五项复审0a9d52f，已核对两个包清单9/9和22/22。配套包的整份JS是旧文件，仅相关函数与当前一致，不能复制为产品底稿；当前源码、发布副本、线上JS的SHA256一致为 `f1d383f87c26019dfccd8e4e103a34863d491f2fe18c6e214952e08ecfef6e96`，CSS为 `7fddbe77650438e1b5a32408adf5edc0dbdeb60af162694ddf389e4c5a694b5e`。

## 已确认根因和最短路径

1. 图表头横向控件区不能收缩，1440/1366真实线上说明区仅46px、工具栏约350px高。仅调整图表内部说明、操作和状态的布局及宽度约束，保留图表实例、取图与缩放。
2. 评分摘要已生成但列表和来源标题不消费。保留三行列表，在身份行及详情来源附近展示现有正式来源分数；有效0可见，缺分不变0，多正式来源不同分分别显示，研究和事故复盘不冒充有效正式评分。旧格式研究摘要可能生成正式分，接通显示前需补身份保护；不改后端评分或排序。
3. decision导航说明被清空。补充正式结果含待确认/不可执行/风险状态，优先关注按现有状态及顺序选至多5项、可能含研究观察的解释，不改集合或排序。
4. 事实按钮统一去price，且全详情首个模块可能属于其他来源。复用现有ref和来源前缀，只在对应目标可证明时生成按钮；价格去本来源价格证据、量能去本来源量能证据，市值/未提供/无可靠目标保留文字。不得将所有本来可靠的路径一律关闭或引入通用模型。

## 保留范围与验收

保留集合覆盖、健康空池回退、罗姐partial、比较文案、手机详情及关闭重开/移除、本来源K线和原价用途保护。缺量能/均线、不能证明的目标和未启用历史比较保持局部空态；不改策略/正式身份/评分算法/正式排序/价格计算，不补历史、不重跑生产、不写正式DB/账本/报告JSON、不发通知。

A批：先复现RED，再修正GREEN；断言正式88/0/缺失/非法值、研究legacy91、事故复盘、同股多正式来源异分及动作分歧；最终列表和详情可见内容必须覆盖，不能只验证scoreText。固定输入的正式/优先/待确认集合和顺序保持，第6项仍可访问。完整页面在1440×900、1366×768、390×844下验证说明与控件可读，有/无均线及量能缺失展开不溢出，实际点击图层/缩放/重置和手机详情关闭重开。

B批：实际点击单来源/多来源对应目标，缺目标无虚假按钮、不跳其他来源或自身；键盘激活、折叠打开和重开后正常。关联原价、异价基不借图、比较移除等保护定向回归，不扩为全历史增强。

影响B02/B04/B06为价格用途、身份和事实显示；B07只恢复现有分数可读性，不改评分排序；B08共享展示消费者；B09运行/数据隔离；B10固定输入、回归和实际验收。B01不补历史，B03不改覆盖，B05不改生命周期。纯展示不要求收益回测。

提交前再次同步main，确认目标祖先关系并重跑相关回归；只提交本批文件，生成资产版本更新必须证明HTML除版本外及报告JSON未变。合并/发布按本批有效授权执行，本地测试不称上线。生产服务禁启；本任务有限测试和浏览器资源结束后清理，仅清理本任务资源。

## 执行证据

方案和初轮证据：`/private/tmp/chanlun-five-review-plan-qb267w3v/`。实施证据：`/private/tmp/chanlun-five-review-implementation-20260914/`。

主进程已冻结用户根、运行树与实施树Git状态/差异指纹，及270个受保护文件。源资产已复制为只读对照。初始四个直接前端模块51项测试通过；后续结果逐批追加，当前尚未写为修复或发布完成。

### 首轮实施与独立审查

实际CLI启动回执已确认 `model: gpt-5.6-sol`、`reasoning effort: xhigh`，命令明确 `service_tier=default`，子代理使用workspace-write沙箱；Fast关闭。初次外层沙箱限制客户端状态初始化后，按授权提升仅客户端启动权限，子代理沙箱未关闭。初轮PID68668/68669正常退出，主进程已用进程表回查。

初轮实施方A新增消费用例2项、B来源跳转4项均有业务RED→GREEN，最终227项定向测试通过。父级独立基线另有189项通过；72项受影响保护组合中1项旧测试仍要求用途未核验原价计算距离。该测试现按已上线用途规则改为原值可读、未核验说明、距离不计算，未恢复危险计算。尚未同步发布资产造成的两项源/副本检查失败由后续资产准备处理；旧借图字面断言与本批新回归分开记录。

初轮独立浏览器已确认图表头恢复正常、手机展开量能缺失信息不挤压控件；实际28根OHLC与原报告精确值一致，不用舍入后的显示价代替行情原值作比较。评分/导航合成页的旧版81项断言为51通过、30预期失败；真实页面旧版最终46项为22通过、24预期失败；这些是独立复现材料，不是修复完成证明。

独立审查仍发现首轮四组本批缺口：空白/数组被转成正式分；legacy显式研究身份被视图formal覆盖；详情分数只在默认折叠来源区可见；来源映射既误关同raw ref的明确选中视图，又可能信任冲突source_refs而错跳。主进程已复跑99项评分断言（93通过、6个非法转换失败）及来源实际渲染探针，保留 `review/candidate-f805*` 失败快照，交同配置Sol做局部修正。合法数字字符串88依据现有前后端数值合同保留兼容，早期把它当非法值的独立测试已纠正并留存旧证据，未以收紧无关兼容要求扩大产品范围。

实际图表/事实跳转验收使用离线完整页面、真实冻结数据及明确合成反例，所有外发写请求被拦截。浏览器测试最初的沙箱启动失败、标签匹配歧义和短暂高亮/动画测量问题分别留证并修正，不计为产品故障。后续验收须核对真实裁剪可见范围、展开/键盘和控件行为，不能只证明DOM/图表实例存在。

### 2026-09-15 最终本地验收与交付

A/B 两批已完成。图表说明、操作与状态纵向排列，密集控件可换行；现有正式分在列表、详情首屏和对应来源标题可见，合法零值保留，多来源分数分别呈现。空白、容器、非有限及越界值不变成正式分，明确研究身份和顶层/来源事故标志继续受保护。导航解释补齐，固定输入各集合及顺序不变。

事实跳转通过已选 ref、evidence_view 和来源映射定位对应来源的价格02/量能05模块。正常第二来源、同raw来源别名和冗余candidate.ref缺失仍保留可靠按钮；明确冲突、缺模块或missing状态降级为原值文字，不跳其他来源。市值只作文字展示。最终两处修正及三个新增测试有独立RED/GREEN记录；旧失败快照继续保留。

主进程在最终源码上独立执行并查看实际截图：

| 验收范围 | 结果 | 临时证据相对实施证据目录 |
| --- | --- | --- |
| 11个相关前端模块 | 264/264通过 | `parent-final-related-tests.log` |
| 评分、身份、导航最终渲染与补充边界 | 99/99、6/6通过 | `review/final-parent-score-render.json`、`review/final-independent-boundary-results.json` |
| 9/14冻结真实报告，1440/1366/390宽 | 46/46通过 | `qa/final-real/results.json` |
| 合成完整均线，保留真实OHLC | 52/52通过 | `qa/final-ma/results.json` |
| 合成评分/导航/顶层事故，桌面和手机 | 84/84通过 | `qa/final-scores/results.json` |
| 多来源事实点击、Enter、Space、展开及重开 | 95/95通过 | `qa/final-facts/results.json` |
| 9/11四只实际K线、手机生命周期、比较移除及价格用途反例 | 29/29通过 | `qa/final-preservation/three-closures-results.json` |
| 生成器及相邻展示扩展组合 | 186/187通过，1条基线旧断言失败 | `parent-final-asset-and-extended-tests.log` |

上述五组浏览器检查合计306项通过，均无页面错误及写请求。真实报告与合成边界分开记录；本轮不是线上浏览器验收，也不证明收益改善或未来调度。后端集合修复保持源码不变，原23项受控探针只证明对应集合/空池/partial分支，不冒充生产全链路重跑。

扩展组合的唯一失败为未修改的 `tests.test_report_generator.TestAccessControl.test_v2_asset_uses_correct_raw_pools_and_kline_order`，要求源码包含 `findChartCandidate(targetCode, found)`。从Git读取基线0a9d52f与最终文件逐条核对，7个字面断言中仅该字符串在两版均不存在，其余6项两版均满足；这是此前关闭不可靠借图后的旧断言，不是本批回归。已保留断言和失败记录，没有恢复错误路径；本来源K线、异价基不借图和未核验原价保护由上述实际29项回归覆盖。该旧测试整理列为后续维护，不称扩展套件全绿。

最终JS SHA256为 `6f375967136d54fba59025815982381870af43d23dcc968835e258839a73154b`，CSS为 `44d90e9d8613ffa3bbc4e28e99d3917989cfaf5d8614d68dbcfd418fbce6ee89`。`docs/assets`与源码逐字节相同，资产版本 `ae767dacf97d`；14个HTML只更新真实资产URL的版本属性，88个报告JSON保持不变。未生成或替换日报派生投影。原用户工作区及production-runtime改动保留。

允许的局部限制是没有可靠目标时保留事实文字、缺量能/均线只降级对应图层；不将研究分转正式分，不合并多来源异分，不启用跨来源借图或未核验价格计算。当前完成的是隔离代码、测试、静态资源和本地验收；未合并main、未推送或部署，运行版本仍为0a9d52f。发布需对应授权，不能从历史展示任务权限自动推导。

三个Sol实施进程均正常退出；最终PID83500/83501及其进程组已由主进程回查退出，所有有限浏览器运行均正常结束并关闭浏览器。未启动业务服务、监听端口或生产后台线程。复现材料与截图作为证据保留，不删除原失败记录。

提交前再次fetch确认origin/main仍为0a9d52f，目标分支为当前HEAD祖先；同步后264项相关测试重新通过，日志 `precommit-after-fetch-tests.log`。源码语法、暂存/未暂存差异检查通过；当前规则及引用方案受跟踪。独立文件审计另确认根/runtime状态和脏改动指纹不变，14个HTML按真实资产属性规范后整份字节与基线相同，非本批数据与代码保护保持。此次仅在隔离分支提交上述代码、测试、资产版本与两份实施记录。

### 2026-09-15 用户授权上线

用户在收到 `e50e2ef4` 本地提交、验收结果与尚未部署说明后明确回复“上线”，授权本批已验收代码/资产进入main并切换production-runtime，随后核对GitHub Pages实际产物与桌面/手机操作。本条替代本批前述“尚未授权发布”的状态；不新增补历史、重跑日报、正式数据/账本写回或通知权限。

发布前重新fetch，origin/main与runtime仍为0a9d52f；实际主机yangfandeMacBook-Pro.local，日报/预跑/核对三个既有调度均not running，无活跃项目进程。保留用户根的全部脏改动。runtime比较页的既有删除外部图表脚本及缩进变化与两个资产版本属性相邻，需要有界合并；先冻结原文件与三方结果，只叠加本批版本值，不提交该原有修改。88个报告JSON逐文件冻结，资源按已验收哈希核对，禁止用诊断数据重建页面。

发布按最新main祖先关系和定向回归通过后推进，运行目录在共享发布锁内切换；不停止或重启已有服务。记录远端main/runtime提交、Pages构建回执、真实HTTP资源哈希和浏览器可见/交互证据。仅当这些实际完成后才称上线成功；源码可撤销本批提交，资产及14个HTML仅撤本批版本变化，保留之后新增报告与原有脏文件。本次发布证据目录为 `/private/tmp/chanlun-five-review-release-20260915/`。
