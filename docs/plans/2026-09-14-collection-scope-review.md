# 2026-09-14 集合口径专项 Review 落地

## 本批授权与基线

用户在核验与方案交付后明确要求“落地实施”，确认本批 R1 覆盖率、R2 健康空集合回退、集合关联审查规则三个目标。主进程保持当前配置，代码落地使用 GPT-5.6 Sol / medium，Fast 关闭；实际启动配置须核验。用户根目录和实际运行目录已有其他改动，均保留。

- 初始 origin/main 与 production-runtime：`5ad058c08a5d89bbca315964476c8cca6b053281`。
- 实施树：`.worktrees/collection-scope-review-20260914`，分支 `codex/collection-scope-review-20260914`。
- 已读根、运行树、实施树 AGENTS.md，初始 SHA256 为 `3dccacd0290ef3c49c0ea8921a31240d8a3e86c1aa785ab202c9f7f4b74785a4`；选股方案第 12–14 节和前次板块完整性记录均已核对且受跟踪。
- 证据包：`ChanlunStrategy_集合口径专项Review_证据包.zip`，manifest 的 9 个文件校验一致；以 8563ade 报告为权威，旧 a0edd61 结论保留历史用途。
- 诊断与本批执行证据：`/private/tmp/chanlun-set-review-20260914/`。原始接口冻结输入沿用 7 板块16页，SHA256 `5a19c3fb5aad03baaadfad6eb192471f9b9a0d731d92bb6885fcb7468fb2abb8`，不新增行情请求。

## 最小关联链与允许差异

R1：`fetch_sector_stocks → collect_daily_data / run._complete_sector_component_evidence → _sector_component_evidence → deduplicate_sector_hierarchy`。当前完整15A+5B仍算0.75并标证据不足；7个真实冻结板块目前均高于0.8，未证明它们因此再次发布失败。

抓取器提供实际原始有效成分代码，两处包装携带明确的原始全成分证据；层级覆盖和重合关系使用同范围代码集合。现有 `component_codes` 保持 A 股范围，供 `build_sector_heat_snapshot` 的涨跌/涨停统计使用。`requested` 仍为接口总数，`unique`/`filtered_unique` 仍为返回 A 股数量。只有数量、缺集合、重复、数量冲突或取齐失败不能自证充分；不降低80%门、不填造身份、不静默重定义旧字段。

R2：已请求的全部预定板块完整且业务空时，保留空股票池，跳过行情库/缓存的故障恢复。任何真实不完整、失败或跳过请求保留原回退，避免 `all([])` 误判；资金流静态兜底身份单独保留，不强设 `is_official=true`。此条件会移除错误恢复的范围外输入；它不代表后续全A/个人观察路径必须为空，不改这些路径。

规则：在 AGENTS.md 增量追加集合变化须核对直接数量/进度/覆盖/状态/回退消费者的条款，不扩为全仓重审。

不改全A抓取与收盘入库严格数量合同、策略参数、评分/正式排序、价格用途、正式库/账本/报告或通知；不重跑生产、不补历史、不部署。R3仅记录为后续公共分页复用时必查事项。

## 实施与验收

1. 在隔离树运行相关测试建立基线，添加能击中当前缺陷的正常/混合组合测试并确认预期失败。
2. 子代理修改 `chanlun/data_fetcher.py`、`run.py` 及直接相关测试；两个产品问题共享文件，顺序实施，避免并行覆盖。
3. 检查原始集合包装和消费者一致性，保留原有缺页、重复页、畸形代码、身份、正式门负例；验证A股候选及涨跌统计不变。
4. 主进程独立复跑真实7板块输入，比较具体股票内容/顺序/请求页及允许改变的层级证据；独立核验健康空、真失败、部分失败、连续失败跳过、缓存/库分支、前空后有A等条件。
5. 回归以 data_fetcher、market_data_guard、run_sector_evidence、sector heat、identity、preclose 和直接run消费者为范围；原始接口回放不等于正式日报或真实数据库验收。
6. 独立审查实际差异和范围。提交前同步目标分支、确认祖先关系、重跑相关回归；只提交本批文件与新增规则/方案。合并和部署状态分别记录。

影响 B03 集合/覆盖、B06事实语义、B08共享入口、B09运行隔离、B10证据；B01无历史补数，B02无价基改动，B04/B05无策略身份/生命周期变化，B07不改评分或正式排序。

## 实际执行记录

实施子代理使用独立 CLI 进程，启动头已核验 `model: gpt-5.6-sol`、`reasoning effort: medium`，命令显式指定 `service_tier=default` 并不加载用户角色默认配置；保留 workspace-write 沙箱。初次客户端在外层只读状态目录初始化失败后，按授权运行客户端，子代理自身沙箱仍限定本实施树和临时证据目录。主进程当前配置未修改。

实施树追加集合规则后的AGENTS SHA256为 `9ad2a840d884c5e74729dcf116ddd3f2876d25cbfbb715269bc5acd26e58d1b0`；根及运行树规则未改。只读规格审查已按新规则复核；子代理实施前重新读取本树规则。

主进程隔离基线两组共213项通过（61+152，0失败/错误/跳过），数据库/缓存/输出均指向临时目录，封锁真实网络、非临时SQLite连接，退出无遗留线程。新增父级6项独立验收在旧版出现8个预期业务断言失败（含4个健康空子场景），0执行错误；记录为 `parent-acceptance-red-tests.json`，不把设计中的RED当作实现已通过。后续须记录GREEN、真实固定输入前后对照、独立审查及交付版本。

第一阶段R1/R2已实现，父级6项业务验收均GREEN；真实7板块取数结果、完整股票字段/顺序与请求页与基线相同，层级证据使用原始集合。首轮子代理核心124项和直接上下游59项通过；旧“4码/requested5/complete=true”要求去重的测试更新为证据冲突负例，原0.8覆盖仍可见但不参与去重，未删除保护。

独立审查发现包装层只保留list/tuple会把显式非法raw字段抹掉，变成可接受的旧格式缺字段；父级复跑字符串/字典/None/整数×两个真实包装器，共8个合成场景确认条件性误放行。此为本批明确要求的异常集合传播边界，继续由同配置Sol/medium修正；不作为已发生生产事故。记录保留在 `r1_wrapper_invalid_results.json` 和 `parent-r1-wrapper-invalid.log`，最终必须通过相反方向的负例回归后才交付。

已核验的真实固定输入对照：

| 板块 | 原始成分 / A股候选 | 旧层级覆盖 | 新层级覆盖 | A股记录/请求页 |
| --- | --- | --- | --- | --- |
| BK1216 | 511 / 507 | 0.9922 | 1.0000 | 不变 |
| BK0545 | 259 / 257 | 0.9923 | 1.0000 | 不变 |
| BK0438 | 130 / 128 | 0.9846 | 1.0000 | 不变 |
| BK0546 | 17 / 15 | 0.8824 | 1.0000 | 不变 |
| BK1208 | 78 / 75 | 0.9615 | 1.0000 | 不变 |
| BK0737 | 134 / 133 | 0.9925 | 1.0000 | 不变 |
| BK0459 | 67 / 67 | 1.0000 | 1.0000 | 不变 |

上表仅是采集/层级证据对照，不是收益回测或真实日报发布验收。两个同A异B板块保留、完整集合相同才去重，以及A股热度的total/up/limit_up不纳入B股，均由独立消费者组合用例验证。

提交前同步记录：origin/main在实施期间新增 `6223ed621c5652076f7566f84ab7291d8b89396b`（罗姐研究池partial发布保护）。主进程在本隔离分支fast-forward纳入；共同选股方案先保留本批独立追加，再在最新文档上追加，保留对方演进记录，未stash/reset或覆盖产品改动。data_fetcher.py/run.py本批审查hash未变，工作树基线现为6223ed62；同步后包含该新测试模块重新回归，结果另行追加。代码修正子代理不负责Git同步，仍只处理已授权产品范围。

## 最终本地验收与交付

包装修正采用字段存在性传递：合法列表复制，显式非法值保留给helper拒绝，只有真正缺字段进入旧证据分支。修正RED为2个方法/8个业务子场景失败、0执行错误；GREEN7项通过，直接相关172项通过。独立规格终审复跑原8场景的反向断言，全部保留非法字段、拒绝充分证据且不错误合并板块；旧纯A正例继续可用。

提交前又同步 `72b9b4a659d23826f190bc6e0fcb6e8d26363cdd`（其他任务自动更新9月14日日报），仅7个正式展示/数据产物变化，本批产品/规则hash不变。所有这些日报产物都按最新main保留，没有用本批诊断包或旧页面覆盖。此前运行目录中4个报告文件的变化属于该已存在的自动日报提交，本任务没有写运行目录；用户根工作区33个已记录状态项逐项保持原样。

主进程在72b9b4a同步后完成301项相关及集成回归，0失败/错误/跳过、0遗留线程，包含最新罗姐partial模块和6项独立业务验收。原始7板块16页再次回放，A股完整记录/顺序/请求页仍与冻结基线逐项相等。六个变更Python文件编译及Python3.7语法检查通过，`git diff --check`通过。验收是隔离的代码/数据合同检查，不是实际日报重跑或上线验收。

独立规格终审25项、代码质量终审12项均通过，无本批剩余阻断。两次审查的产品hash与最终主进程验收一致：

- `chanlun/data_fetcher.py`：`9261176c060245e996a208217c41dc32cda95e9ca9e56b7851e6e8d9c2dd7ceb`
- `run.py`：`ce728624e211dabe1a38fd6bc4732b0726e03fa21469211f1409e79356b91110`

相对最终main基线仅4个函数变化：`fetch_sector_stocks`、`_sector_component_evidence`、`collect_daily_data`、`_complete_sector_component_evidence`。全A/身份函数与收盘入库、config、A股热度实现，以及并行罗姐修复的report_generator/validate_today_report均保持原样。本批共9个文件，产品2个、测试4个、规则及方案3个。

证据索引（本机 `/private/tmp/chanlun-set-review-20260914/`）：

- `parent-latest-final-tests.json` / `.log`：同步后301项最终回归。
- `parent-real-fixed-input-comparison.json`：7个真实板块同输入对照。
- `r1_wrapper_invalid_green_results.json`：8个包装异常场景关闭。
- `reviewer-final-scoped-tests.json` / `quality-review-tests.json`：独立审查回归。
- `final-function-scope.json` / `final-compile-source.json`：变更边界与编译hash。
- `worker/handoff.md`、`worker-correction/`：两阶段Sol实施的RED/GREEN与实际启动证据。

交付按本轮已确认范围保留隔离分支和工作树，不合并main、不发布、不重跑、不通知。R3全A接口复用审查保持后续事项。没有启动业务服务或监听端口；测试临时库/缓存/输出目录自动释放，实施与修正进程均须由主进程核对退出。实际分支提交号以Git记录和最终交付消息为准。

资源最终回查：主进程在实际本机核对两个实施进程组均无剩余PID，记录为 `final-process-cleanup.json`；工作树未产生.cache/output_debug/__pycache__目录。用户根工作区的staged和unstaged差异hash与开始时完全一致；未跟踪目录未进行递归内容比对，本任务未向其中写入。
