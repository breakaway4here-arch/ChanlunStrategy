# 项目审查问题逐项修复与上线准备计划

**目标：** 将本任务审查列出的九项问题及手机首屏摘要完成修复、影响验证和上线准备；不以单项通过替代完整交付。

**架构：** 以实际运行版本和最新 origin/main 的 0e5225e 为冻结基线，在独立工作树实现。先统一证券身份、量能与评测事实，再修评分及研究结果流转，最后补足展示合同。策略行为变更使用已有冻结输入和可信数据做原版/修正版对照；污染数据另形成可核验的迁移批次。

**技术：** Python、SQLite、现有 unittest 与前端 Node 测试、静态 HTML/JavaScript 工作台。

## 授权和隔离

模型执行勘误：用户后续要求所有子agent关闭Fast。核对session上下文发现，此前Sol/medium派发参数被通用角色文件覆盖，实际为Luna/max；主进程已告知用户并停止旧代理。三类通用角色现已持久改为标准速度，通用模型默认保持原样；本任务后续以临时执行配置显式锁定Sol/medium/default并核对启动信息。此前“Sol实现”记录仅代表派发意图，不能作为实际执行型号证据；源码和测试证据保留，未完成部分由已核对配置的执行补齐并独立验收。

- 用户已在本轮审查结果后明确要求“挨个修复吧，修复到可以上线”。该指令授权本计划的代码和测试实施，不把计划撰写作为需要再次确认的停止点。
- 主进程保持当前配置，落地代理使用本任务已记录的 GPT-5.6 Sol / medium；主进程独立验收。遵循 AGENTS.md 和 2026-09-10 总方案第12—14节；旧逐批等待记录不覆盖最新任务授权。
- 实施工作树：`.worktrees/project-audit-fixes-20260911`，分支 `codex/project-audit-fixes-20260911`。主目录已有修改、实际运行目录与已有研究证据受保护。
- 不继续大规模历史补数，不直接写正式行情库、账本、报告或通知状态。需要历史修复的部分，先用已有可信素材生成精确旧/新值及可重复执行的迁移方案，在隔离数据副本演练。
- “可上线”必须有源码、测试、固定输入变化、数据迁移预演、实际页面与回退证据。真实发布、正式数据写回和用户接收分别记录，不以本地绿色结果代替。

## 问题与验收清单

| 项目 | 修复合同 | 主要文件/路径 | 完成证据 |
| --- | --- | --- | --- |
| R01 证券/指数身份 | 资产类型、交易所、代码贯穿行情路由、缓存及存储；同号股票/指数不得串线；已污染记录有精确修复批次 | data_fetcher.py、kline_repository.py、market_history_store.py、market_close_snapshot.py、相关调用方 | 000001/000063股票与指数分别路由及存取；真实污染样本旧新对照；迁移幂等/旧值冲突/回退测试；指数原入口不退化 |
| R02 趋势评分 | 统一上涨/上升、盘整/震荡等规范值；缺失个股趋势不能由市场阶段冒充；保留风险门 | decision_engine.py、相关信号装配、tests/test_decision_engine.py | 原错误负例、上涨/下跌/盘整/缺失正负例；冻结候选全分项和最终动作对照 |
| R03 预跑板块排名 | 显式板块排名有唯一口径，不由逐股输入位置制造排名；与正式入口一致 | preclose_pipeline.py、scorer.py、run.py适配处 | 冷/热板块原反转负例；股票顺序置换不变；重复/缺失排名；同输入双入口对照 |
| R04 回测成熟度 | 未满周期不能填作T+3/T+5；按实际交易日、有效后续记录判断成熟，汇总分母按周期正确 | backtest_execution.py、backtest_metrics.py、historical_experiment_metrics.py、policy_experiment_metrics.py、调用脚本 | 1/2/3/4/5日边界、缺日/停牌数据契约、收益/退出模型调用回归；正式记分牌原保护不退化 |
| R05 量能单位 | 用同日原始证据确认供应商单位，读取/入库统一；缺失成交额不能假装真实零；旧源混合数据可追溯修复 | data_fetcher.py、kline_repository.py、market_history_store.py、成交额/量比消费者 | 供应商同日对照；来源切换前后量能连续性；已有真实样本资格/量比/排序影响；数据修复预演 |
| R06 独立研究策略 | 罗姐等研究策略保留自身命中及共同限制的不同语义；研究评估不再被错误呈现为自身无信号；正式推荐身份不自动扩大 | run.py、report_view_model.py、研究合同及账本相关模块 | 9→0真实案例逐层解释；独立研究组与原组对照；正式动作与账本隔离、无观察转正式 |
| R07 覆盖披露 | 空池结论与首屏同时说明正式输入健康及检索/分钟覆盖缺口，分母含义一致 | report_assets/report-v2.js/.css、report_view_model.py、report_generator.py | 9/11的470/110、274/336真实渲染；正常完整/部分/系统阻断场景；旧报告缺字段降级 |
| R08 观察条件 | 明示缺少什么结构锚点/条件；有验证锚点时保留来源、日期、用途，普通参考价不自动当失效位 | decision_workbench.py、report_view_model.py、report_assets、上游观察数据合同 | 20只有文字无观察价真实案例；缺锚点仍不冒充可执行；合法锚点进入等条件；失效/过期不复活 |
| R09 跨期对照 | 从策略运行身份与真实价基生成稳定比较合同，不依赖当日是否有正式候选；逐期变化可解释 | decision_workbench.py、report_generator.py、策略运行manifest相关装配 | 连续日期真实数据对照；空池仍有版本；版本/价基改变不强行比较；单票缺证范围正确披露 |
| R10 手机首屏摘要 | 首屏可读结论与覆盖摘要，大盘证据继续完整展开 | report_assets/report-v2.js/.css | 桌面和390×844手机实看、键盘/缩放/横向溢出、跳转至完整证据 |

## 实施和证据顺序

1. 冻结主目录用户路径指纹、运行源码/报告/库身份；验证最新目标基线。建立只允许临时库、阻断外网的测试环境，先跑当前基线。
2. 数据身份和量能先做调用链、源证据及迁移范围核对；每个具体缺陷先新增能捕获旧错误的测试，再实施。不得依据价格外观猜证券身份或批量改价基。
3. 修趋势、板块和旧回测入口。各自保留最小负例，并用原输入复算新增/移除/动作/排序原因；事实纠错不伪称收益提升。
4. 独立研究路径与观察条件先冻结合同和正负例。保留完整候选来源及限制，不用研究结果替代正式推荐。不能只换文案掩盖依赖链仍未修复。
5. 补覆盖、比较与手机摘要。通过现有生成入口构建完整测试产物，核对真实数据，不只用字符串测试证明页面可用。
6. 独立规格审查→代码审查→修正→主进程复现。提交前再次同步目标分支，确认祖先关系并重跑相关回归；合并所有依赖后运行完整测试。
7. 对R01—R10逐项记录源码位置、原失败、新通过、固定输入影响、缺样本、成本、运行隔离和回退方式。任何缺项仍标未完成，不能以其余项通过结束目标。

## 本轮进展

- 2026-09-11：运行工作树干净且HEAD=0e5225e；已fetch核对origin/main同版本。独立实施工作树已建立。主目录已有修改保持原状。数据修复证据及行为回放设计正在独立只读核对；尚未宣称任何修复完成。

## R06 独立研究路径的具体实施合同

主进程已从当前冻结库重建9月11日原58个15分钟目标，58个均有完整本地数据且主题匹配；实际调用原罗姐分析函数得到的9只与原报告中被最终共同上游全部排除的9只完全相同。证据：`research/project-audit-fixes-20260911/parent-luojie-recorded-cohort-reconstruction-v1.json`。元数据由冻结预跑及数据库行业资料装配，来源差异保留，不能宣称所有中间评分与15:20原执行逐字节相同。

1. 罗姐是独立研究身份。它的主题和15分钟形态应在共享日线健康、资格及风险约束后的候选全集上评估；不要求同时属于经典纯净/底部形态池。正式日线融合、H4、经典启动的既有准入不借本次研究修复扩大。
2. 去掉研究自身入口及最终展示对`pure_pool/pure_scored`的错误交集依赖。保留原交集关系为诊断，不把交集失败说成罗姐自身无信号。
3. 将研究15分钟获取放在正式30分钟任务之后，使用独立、明确、可配置的有界预算。预算筛选采用有证据且稳定的优先级，不依据股票字典顺序；未请求、预算排除、取数失败、数据有效但无形态分别计数。实施者先用当前检索/主题集合核算目标数和原请求数，再给出具体预算与成本说明。
4. 一个研究股票缺15分钟时，仅该股票待证据。实际通过输入核验的研究候选可继续展示，清楚标注部分覆盖；全市场共享数据/价基故障仍阻断。不能用“partial”悄悄取消正式策略的健康门。
5. 新研究范围使用新且可追溯的策略/政策身份，旧账本和历史报告不回写。工作台、通知、正式ledger贡献和scorecards的role契约必须证明研究不升格为正式动作。
6. 用原58目标的9只真实正例、已确认失败形态、缺分钟/过期/非final/预算不足负例，验证自身候选、研究输出和正式输出三个边界；成组回放仅使用已有材料，不伪造新请求成功或收益优势。

本补充把审查中“先显示漏斗、再验证独立评估”具体化为独立研究实现与验收，不授权取消正式策略保护或历史全库写回。

## 独立复核追加项（2026-09-11）

以下是原 R01—R10 合同的未完成细节，不扩大正式策略角色或发布权限。v5 局部绿色不能覆盖这些缺项。

- R02：次日爆发与罗姐 builder 到统一决策的真实趋势字段也必须贯穿，不能只修启动/趋势续接两条派生链。
- R03：实际用于评分的规范板块排名、来源与冲突诊断同步到候选和报告；不能分数按 rank2、标签仍显示 rank1。
- R04：历史实验、过滤样本与各回测脚本的归一化必须透传 finality；晋级/覆盖统一使用对应成熟计数，处理总行数独列。无效高低点、未收盘行情不得污染辅助回撤/止损结果。
- R05：缺失额不能被有效子集均值改称完整五日均额；预跑追加今日数据不能把历史未知量/额标成已核验。供应商优先级服从最新日期、窗口有效性和单位合同。
- R07：已知部分资格数量、缺失检索/分钟记录时，整体覆盖不能显示“已核验”；保持缺失和冲突的独立状态。
- R09：策略身份不含每日变化的价格；数值可比性按同一股票的真实价基签名判断，不要求不同股票复权因子相同。历史无价基可做成员级对照，不冒充可算数值变化。
- R10：本机真实手机验收发现长链接挤窄摘要，将其移到正文之后。首屏数量说明使用中文，不展示内部字段名。

新增证据：`parent-review-progress-v2.md`、`parent-ui-contract-repros-v1.json`、`parent-worker-v5.json`、`parent-behavior-v5-bounded-outcomes-v1.json`。具体来源、样本不足和临时资源清理记录保留在研究证据目录，尚未证明收益改善或实际上线。

## 量额合同与最终报告实链复核补充

- R05 独立审查发现 repository/batch/preclose merge 仍有逐行量额元数据丢失，以及流动性、情绪和20日展示量窗对 unknown 的消费漏洞。修复必须从数据来源一直贯穿到消费者；不能用 full-A 的13日资格门替代其他入口及20/21日展示窗口的证据。具体负例、真实603459混合来源及修复边界见 `diagnosis-data/r05-independent-review-v1.md`。当前由独立临时源码副本实施，合入后重跑实际固定数据影响。
- R09 主进程实际 main 复测发现运行清单虽然已计算并传入 scorecards，却未挂载到最终 report_data，展示端只能依赖回退资料，缺少明确的当期身份来源。必须让真实清单经过 main→report_data→生成产物，不能用 staging 重建清单冒充生产链验收。证据 `parent-r06-main-fixture-v4.json/.log`；补齐后 `parent-real-main-ui-regression-v5.json` 的完整/部分研究输入及逐股比较三项独立验收通过。
- R01/R05 联合数据候选现在由同一冻结基线组成57,900项操作，含398项无可信替代的尾部/分钟隔离；原先三份各在独立副本通过的证据不再作为整包可执行的充分条件。必须在同一个副本验证整体应用、重复执行、晚期冲突回滚、反向CAS和回退。未知北交所身份与未覆盖日期保留待补齐，不推断替代价格。
- R10 最新本机浏览器证据 `parent-ui-browser-v5.json`：390与320宽首屏摘要完整、无横向溢出，键盘跳转有效，桌面市场证据保留。浏览器使用本地资源、无监听端口、结束后进程已退出；这是临时页面验收，不代表线上资产更新。

## R05 数量发布边界闭合（2026-09-12 追加）

- P1 A 根因是 `main()` 生成统一 `selection_input_health` 后，下游正式策略项、ledger 与 shadow publication 仍读取统一门之前的局部健康变量。现改为统一门输出覆盖下游有效健康态，ledger eligibility 同步服从 formal gate。11/11 与 10/11 正例保留，9/11 低于既有 90% 门时不再生成 formal contributions，ledger 与 shadow publication 均不放行。
- P1 B 根因是 main/preclose 无条件聚合 formal、research 与 observation diagnostics。现按实际 orchestration role 投影：daily structure/fusion 与 classic formal 输入保留；经典涨停观察代码排除于 formal requirement；右侧仅 active 计入 formal，off/shadow 的原始研究诊断继续可见。未知证据没有全局忽略，非入选 formal peers 仍计入覆盖分母。
- 影响 B06/B09/B10。永久 actual-main 与 preclose RED 已先失败；修复后父级边界 2/2、目标 3/3、相关 228/228、全量安全套件 2172 项通过（0 failure、0 error、1 默认 opt-in skip）。详细哈希、命令结果和隔离边界见 `research/project-audit-fixes-20260911/diagnosis-data/quantity-boundary-standard-handoff-v1.md`。
- 当前仅为隔离工作树实现交付。主进程独立审查、目标分支同步、提交、发布、部署和真实接收仍待完成；不得将本记录写成已上线。

## R01 部分身份缺口集成闭合（2026-09-12 追加）

- 按第 12.2 节的单股隔离边界，收盘快照改用完整 A 股身份分母和未决身份分子记录。11 个健康身份加 1 个未决身份时，原始覆盖为 `11/12=0.916666...`，状态为 `partial`，健康规范身份继续进入 actual main；未决代码保持原身份，不借用 SZ 锚点，也不改名为 BJ。完整输入仍为 `complete`。
- 全局停止仍只由来源不完整、provider 身份无效／重复、非正数历史因子或完整分母覆盖低于既有 90% 门触发。运行门以未取整的 numerator/denominator 复算，不接受仅靠六位小数四舍五入越线，也不允许低于 90% 的配置削弱门槛。现有冻结结果 `5198/(5532+299)=0.891442...` 继续阻断；12:25 归档计数只保留为同一低覆盖证据，不标记为 15:05 新收盘证明。
- 数据库中已知 B 股及旧的 impossible-SH 股票行不属于 canonical A 分母，也不是 provider 完整性失败；公开诊断保留 `identity_invalid_rows` 并增加 `registered_non_a_rows_ignored` 准确说明。provider 本次返回的无效身份、重复身份和不完整 source 仍失败关闭。
- 未决身份过滤移到 `_apply_full_a_universe` 和个人观察池注入之后的最终候选装配边界，防止 `920001` 被晚期重新送入 analyze 或策略消费者；排除代码去重记录。个人观察池配置仍保留，采集元数据改为 `unavailable / identity_migration_pending`，不得伪装成“无确认”。
- 影响 B01/B03/B08/B09/B10。新增永久 actual-ingest→actual-main、late full-A、个人观察池、raw threshold、workbench、manifest 正负例；父级 RED 为 3/3 失败，修复后父级／数量组合 13/13、R01 相关 80/80、完整安全套件 2185 项通过（0 failure、0 error、1 opt-in skip）。运行保护记录到 1 次被测试主动触发并拦截的网络尝试，正式数据库禁止事件为 0，测试期间源码变化、残留线程和子进程均为 0。
- 详细 SHA、RED/GREEN、完整日志和隔离说明见 `research/project-audit-fixes-20260911/diagnosis-data/r01-final-integration-handoff-v1.md`。本轮没有改 HTML、JS、CSS 或 asset query；JS 仍为 `271de56f5beae9004d81b2783b59bbc18ed09a066d2872ec1e733ca58f548122`，现有 query 仍为 `1d1f3d599c1d`。状态仅为隔离集成交付，未暂存、提交、推送、部署，未写正式库／账本／报告／通知；主进程后续独立验收并处理发布，本文不作 GO 声明。

## 主进程最终本地验收与数据等待（2026-09-12）

后续普通速度Sol展示补丁已审查合入：身份收盘核验与量能部分覆盖的具体分母／待核验数量进入手机和完整摘要，未知值不补零。最终asset version为406bebd4c9de，14个入口只更新版本参数并保留原EOF与内嵌事实，替代上一段中间版本。主进程最终2188项组合测试全部通过，包含11日原始冻结输入，0失败／错误／跳过；严格暂存、真实Chrome／ECharts、390／320／1440宽和键盘验证通过。221个任务PID复查已退出，14个正式保护文件与25个用户文件均未变。完整结论见 [最终本地验收与剩余上线条件](2026-09-12-project-audit-readiness.md)。

整体仍为NO GO：冻结输入5198/5831低于90%，正式数据修复与真实入口验收尚未执行。历史扩量仍遵守用户停止要求；已准备10只、最多40次请求、仅研究副本的未执行清单，等待明确授权。本轮不宣称可上线或已发布。

## 2026-09-12 发布准备完成（替代上方当前等待状态）

后续经用户授权的92%补齐已经正式应用，当前实际覆盖5365/5831=92.01%。本轮对剩余57822项以当前正式库快照完成逐项适用性、联合应用、幂等、范围外数据不变和精确回退验证；实际资格名单3481→3481，三种检索排序完全一致。正式路径受限适配器及10项授权/回退测试已合入候选，保留原4项测试。主进程同步目标引用后以最终代码完成2198项安全回归，0失败、0错误、0跳过；Worker本地8项合同和类型检查通过，页面源资产与此前实际浏览器验收一致。

据本计划“可上线”要求，源码、固定输入影响、迁移预演、页面和回退准备现已闭合；正式迁移和部署由后续发布执行记录证明。剩余57822项写入仍遵守独立正式数据授权，不把原92%补齐授权扩大。旧源码与新BJ身份的兼容性约束、执行次序、固定清单和恢复边界见 [发布执行说明](2026-09-12-project-audit-release-runbook.md)。原89.14%、试点等待及v8 NO_GO均作为历史过程保留，本条不声称实际上线或接收完成。
