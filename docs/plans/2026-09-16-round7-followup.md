# 第七轮遗留问题：历史原始条件可读性

## 范围与基线

用户本次明确要求基于最新版本复查第七轮，只修仍存在的问题并上线；2026-09-16 确认实现配置 Sol / xhigh、Fast 关闭。主进程保持当前配置，负责根因、独立审查、实际页面与发布验收。

实施树 `.worktrees/ui-round7-followup-20260916`，分支 `codex/ui-round7-followup-20260916`，起点 `a8db36c2b6a44412f04aa6be5480bee43738d5a4`，与当时远端 main 一致。根工作区和实际 production-runtime 的原有未提交内容已另行冻结；本批不纳入提交。

第七轮后的三个收口点已在 `22e82da4` / `a0edd61d` 落地。当前专项 22 项及图表/价格保护相关 51 项基线通过；线上历史 HTML、JS、CSS 与起点文件逐字节一致。第七轮“未发布”仅是当时状态，原报告不改写。本批不恢复复杂自动合并、跨期价格计算、M5 或全历史增强。

## 已复现问题与最小处理

1. 真实 2026-08-14 `startup_watchlist/300017` 有次日、升级、取消条件；无新版 evidence 时详情提前返回，原价与图表保留，但条件遗漏。恢复三组原文，逐项说明未记录或不可用，明确历史原始记录、未重新确认，不代表当前已满足。
2. 同代码可能在不同来源、同一池多条记录中有不同条件。item 自有有效字段优先；只允许可核验唯一 ref 原记录补缺，不从同代码第一条或其他池拼接。歧义时保留 item 已有内容并局部说明，不让全部详情消失。
3. 实际8/14归档经既有访问校验后可以加载，但旧策略视图缺少新健康合同，候选被封闭，只有“原始池仅保留追溯”文字而无入口。必须提供相关空态下的原始条件只读追溯入口，才算用户可用。当前候选数、正式门、比较集合保持；不把追溯条目重新加入候选、不生成动作、不借图。新的 unavailable/output_hidden 或错误合同不借此绕过。

本批恢复的是原始文字与必要阅读入口，不计算策略结果或收益，不扩大补数，不写报告 JSON、行情库、账本或通知。原有日期、来源、价格用途、当前手机操作与图表需要回归。

## 验收

- 固定真实8/14记录；只含次日条件、部分缺失、空值/坏类型、同代码不同池/同池重复、HTML转义及输入不变等反例。基线失败、候选通过分别留证。
- 实际浏览器通过旧报告视图打开原始条件，桌面和390/320宽确认文字可读；当前9/16主推、K线及详情关闭重开、比较移除保持。
- 同步最新main后重新执行直接回归；发布只同步资产和引用，不重新生成历史事实。报告JSON、HTML内嵌原始对象以及用户工作区保护复查。
- 公开资产逐字节回读、线上实际操作通过才称上线。所有本任务进程退出，临时视口复原。

## B01—B10 边界

B04/B05/B06：历史条件只追溯，不升级身份、不复活事件、不宣称满足。B02/B08：保持来源图表和价格用途保护、相同详情消费者一致。B09/B10：隔离实施、真实发布回读、保留原数据和失败证据。B01/B03/B07：不补历史、不改评估集合、分数或排序，仅显示原记录文字。

## 执行记录

首次原生子任务派发请求 Sol/xhigh，但实际 turn_context 显示 Luna/max，已在产品修改前停止（当时隔离树仍干净）。随后用 Codex CLI 显式 `model=gpt-5.6-sol`、`model_reasoning_effort=xhigh`、`service_tier=default`、workspace-write 启动，启动回执与 turn_context 已核对模型和推理强度。后续只使用确认的配置。

实现、独立验收、提交与发布结果待实际执行后追加；本段不作为完成证明。

### 实现与发布准备

最终源码 JS SHA256 `ce80fcbfbb26eb330fe28d815cfe163154794d8dbd45b73df52ac3a6b70585da`，资源版本 `7628fb335e79`，CSS 未改。主进程提交前再次同步 origin/main，起点仍为 a8db36c2；291 项直接回归及资产一致性检查通过，退出码0。独立 Sol/xhigh 只读审查针对同一最终哈希，228 项相关回归通过，未发现本批阻断。基线同输入确实没有条件追溯，当前真实8/14待确认原记录26条可读且候选仍0；这些为离线消费者验证，不代替浏览器。

主进程新增边界复现已收口：异常来源构造键不再导致追溯崩溃或显示内部函数；显式未知健康合同不误作历史缺失；原正式状态筛选空态保持。源码只增加只读条件读取与空态追溯，不调用旧参考位/决策helper。缺失条件组不编造，重复来源仅保留item现有字段，不猜raw首项。

15份HTML经去除资产版本后逐字节相同；90份报告JSON哈希不变。宽跑曾有两项资产未暂存失败，现已通过；另两项在未修改a8db36c2上独立复现：旧测试仍要求跨池借图，另一个event-risk文本合同断言原已失败。本批不恢复借图、不改无关event-risk逻辑，不把宽跑称为全绿。

实现轮次的补充通信短暂产生两个同配置执行者，已停止原CLI执行者并由唯一实现者整合测试；最终diff与验证只针对上述稳定哈希。首个错误模型子任务未产生产品改动。运行目录原有删除归档页及比较页编辑继续保护，提交不包含这些内容。

尚待：提交发布、公开资源读回、真实桌面/手机操作和资源清理。
