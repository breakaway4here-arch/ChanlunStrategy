# 第七轮后三个收口点实施与发布

> **授权适用范围（2026-09-14 规则整理）：** 本文仅记录既有展示任务三个收口点的继续实施授权及验收要求；依根 `AGENTS.md` 的长期口径按本批影响交付，原轮次和失败证据继续保留。不将此许可扩展到新任务、策略、写库、补数或通知；本次规则文档提交不触发本文产品实现或发布。

最新用户授权替代限定范围内第七轮后停止修改的限制，允许完成必要修正、测试与独立审查，不逐次申请轮次。原七轮报告/失败证据不改，实质批次从累计八继续记录。范围外能力、补数、策略或显著资源扩张不在本次授权内。

## 固定起点

分支 `codex/ui-availability-release-20260914`，HEAD/已更新 origin/main 均为 `aa991b9acb8e2c69b5b4b4ffef9f750f2a4c3de3`。第七轮 JS SHA256 为 `9736ae6eb7afc5631e03ee6445ee3ec41a809f59f6966142f6178930876e08a2`，旧报告及快照存在 `.cache/ui-availability-release-20260914/`。保留第七轮通过的手机和主要操作，不整体返工。实现沿用 Sol/xhigh、Fast关闭；主进程核对差异并独立验收。

## 三个明确切点

1. `findRawCandidate` 与 `mergeChartCandidate`：按实际 ref 和对应报告读取同来源 K 线，关闭跨来源借图和无法定位 ref 后按代码取首项的兜底。本来源无 OHLC 只图表空态；禁止新建价基兼容、复权换算。
2. `getCandidateReferencePriceForCalculation`、`renderChart` 和实际统一详情：明确用途未核验的原价保留带用途说明文字，不可进入参考线、止损、收益或相关计算。有效现价和合法正式价格不应被无关缺值吞掉。
3. legacy/incident 无 evidence 详情早退路径：提供原价文字替代并明确仅追溯/用途未核验，保留 OHLC；不扩改全部历史条件字段。

## 验收和发布

沿用冻结失败输入做前后对照：同日 A qfq55.99 无图、B hfq559.9 有图；unverified contract10 与现价11；incident原价15.2。再验证真实000636/002600/301682/600184已有 ref 和OHLC、列表/比较/当前变化入口、390/320手机可见图、关闭重开、移除重选。使用浏览器实际可见内容，隐藏实例不算通过。

更新被此次明确要求替代的旧测试，保留失败日志和新旧预期说明。复杂归约、全历史差值/旧图和非必要字段继续标暂不可用，不把原40条全部标完成。数据/配置/库/账本与原工作树保护不变；不触发报告重算、通知或补数，无M5。通过后复制实际发布资源并仅更新资产版本，核对HTML原始对象不变；同步main后提交、合并、发布，回查线上资源和真实手机操作。

服务限制不变：本机不启动监听服务，仅离线路由浏览器或现有线上页面。测试记录PID并清理本任务进程。

## 发版候选验收记录

2026-09-14：保留第七轮手机布局及操作，本批完成本来源取图、未核验原价文字展示与参考线/计算隔离、事故原价文字替代。无本来源K线只图表空态；没有建立价基兼容或复权换算。用途必须明确为 verified/formal 才允许进入对应参考价计算；合同缺价不借原价，研究观察来源原价同样可读。

最终源 JS SHA256：`e74f308b738c5bc421aa075743d05d2650d0e26703638ea2dc301c8e318d0512`，CSS 未修改，资源版本 `be21dc06b109`。主进程 232 项相关回归、5 项资源暂存测试、独立22项语义验收、390/320手机13项实际操作与三种受控输入通过。真实000636/002600/301682/600184的50/80/28/50根K线与末根值匹配；异价基不借图；原价10与15.20文字可读且不画未核验参考线。

过时测试已按本次授权更新：不再要求跨来源借图、事故原价参考线或删除冲突版本；保留本来源正例、价位用途和原值文字负例。原七轮记录、首个候选adb失败、3a缺用途状态失败及工具格式/样本校准原件分别保留；本批含验收发现后的必要修正，未称一次修改全部通过。详细变更序列在本地证据目录的 implementation-log.json。

新增文档基线 `ffad11224e035fbf0c8cf802eaeff8f7f3b96449` 只包含规则和本批文档，产品基线仍aa991b9；已按新规则复核启用范围。复杂自动合并、完整历史差值与历史图表增强仍未启用或延期，不将旧40条全部标完成。B02/B03/B04/B06/B08/B09/B10仅涉及展示读取保护；B01/B05/B07策略行为不改。线上状态以本批发布回执为准，本段不将本地通过当作已发布。
