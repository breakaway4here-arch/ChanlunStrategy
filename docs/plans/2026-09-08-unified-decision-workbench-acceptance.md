# 统一决策工作台：修复与本地验收

2026-09-08。用户在 Spark 额度不足后授权主任务直接接手。实施基于最新 `origin/main` 的 `4015041`，迁入 `f649af6` 图表中心改版，保留后来的历史归档修复。原始工作区及 Spark 工作区均未用于本轮改写。

## 实际行为

- 网页和盘后通知共用按交易日、阶段、快照、标的建立的展示投影。同股一行，主推与 H4 各自保留正式动作、分数和合同；研究来源不能覆盖或否决正式策略。
- 首页先给本期结论及可核验的筛选解释，再按优先关注、全部、正式结果、等条件、待核验/风险导航。优先关注最多 5 只，条件不足时允许为空。
- 详情先显示股票状态与真实 K 线，再说明为什么关注、能否执行、下一确认、失效条件。各来源完整证据折叠保留。
- 全局/个股证据过期、非终局、价格冲突及策略分歧均阻止可执行状态；缺价保留正式动作，同时明确缺失字段。非法数值和布尔值不能作为价格。
- 通知抑制 pending；重复运行、文案润色和排序变化不重发；各渠道分别重试，兼容旧成功记录；真实条件更正明确标注。H4 独立合同的变更也参与判断。
- 预跑按标的去重，整条消息最多 3 只，保留截止时间。对账超过预算记录未发送状态，不延长截止时间。

## 验收证据

| 范围 | 结果 |
| --- | --- |
| 全量 Python 回归 | `python -m unittest`：1,945 项通过，最终运行 34.996 秒 |
| Worker 回归 | `npm --prefix cloudflare/top10-worker test` 通过 |
| 前端语法及差异检查 | `node --check chanlun/report_assets/report-v2.js`、`git diff --check HEAD` 通过 |
| 9/7 真实报告 | 全部 28 个标的；优先关注 0；正式结果 0；等条件 0；有风险标记 21。11 → 2 → 0 的解释来自现存筛选记录，未将观察对象补成正式候选 |
| 1440×900、1366×768、390×844 | 真实页面及完整、缺价、陈旧、无图四类固定样本均走查；无横向溢出。1366 桌面选中研究对象时日 K 主图区进入首屏 |
| 手机交互 | 详情展示 K 线、成交量、MACD；关闭返回所选股票；切回桌面后图表重新挂载 |
| 历史兼容 | 9/4 页面继续显示 9/4 数据，明确旧版报告能力，不借用 9/7 投影。访问门保持原规则；本地历史页走查使用隔离的授权样本 |
| 通知 | 全部使用模拟发送；验证旧记录升级不重发、条件更正、重复重试及渠道失败；未发送真实测试消息 |

固定样本均明确标注“验收固定样本（非正式推荐）”，只存在于私有本地预览，不进入提交。图表使用存档真实行情，缺图场景删除图表输入以验证空态。浏览器截图已在审查会话中展示。

资产版本：`0727dc65044b`。源资源与 `docs/assets` 逐字节一致：

- JS SHA-256：`49ff87eeaa408621a5161e5af6fe4d52d71d88d641a32032c01de888f63c1a25`
- CSS SHA-256：`52e1a0f80193689664d0d813d1e5258260627d73906142475089baecf38623b2`

受控 staging 只允许 `recommendationEvidence`、`decisionWorkbench` 两个派生字段及资源版本变化。受保护文件前后 SHA-256 一致：

- `docs/data.json`：`33228dc5ed4abb47d9117408a04d57e0c7abf89e6783f62e46686e4ee1ff852f`
- `docs/data/2026-09-07.json`：`b508f3b0294fa6cfff743bf728025e81fea27775a88d8dcfd0b51caede5e10f4`

未改选股阈值、行情、正式候选、账本、数据库或持仓配置。没有运行正式日报、历史修复或真实推送。

## 交付边界与回滚

M0–M4 的本地代码、集成及浏览器验收已完成；AC-20/M5 线上版本验收、运行环境代码更新及真实渠道实收仍未完成。历史页采用保守兼容模式，本轮未重建历史正式事实。首次升级不会为了换文案重新发送已经成功的旧通知。

发布时应先更新运行环境代码，按现有流程发布 HTML/JS/CSS，核对根页与日期页实际日期、版本及快照。不要重跑 `daily_run.sh`、改写历史账本或触发真实测试通知来刷新界面。回滚使用本次提交的逆向提交恢复代码和展示资源；如期间有新日报，重新按该日受保护事实暂存页面，不能覆盖为 9/7 旧数据。通知 outbox 保留，不删除发送记录。
