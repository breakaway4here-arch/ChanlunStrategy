# 展示优化 v1.0 实施与验收记录

## 目标、授权与边界

2026-09-12 用户交付《ChanlunStrategy 展示优化最终方案 v1.0》，随后明确授权新分支实现、子 agent 编码、主进程回收审查，最终合并 main 并上线。完整范围为 U01—U16、M0—M5、AC01—AC27，不将首批完成等同全任务完成。

- 主进程保持当前配置，负责拆分、独立验证和发布验收；本地配置读回为 gpt-6-astra / ultra。
- 本任务实现 agent 显式使用 gpt-5.6-sol / xhigh；不启用 Fast。本地 service_tier 读回为 default，不更改该配置。此授权仅用于本任务，替代旧任务的 Sol / medium 记录。
- 每个具体子问题最多 5 轮实质修正（含自审与审查反馈）；达到上限后记录证据、影响、遗留原因，隔离或撤掉不安全改动，继续其他任务。不得以隐藏失败、弱化测试或改写需求达到通过。
- 延续蓝白主题和现有 JS/CSS，不迁移框架，不新建策略/行情系统，不扩大历史补数，不执行 daily_run、不写正式账本、不重发通知、不保存自选或持仓。
- 纯交互 A 类保持完整 bootstrap 不变；B/C 类新增只读派生/数据能力必须给出字段白名单，其余业务事实不变；D 类策略或外部行为不在本方案内。
- 保留六指数、正式情绪五项事实/分项分数、20 日曲线在正常文档流默认可见，保留正式/研究/自选/持仓身份，K 线仍为个股主视图。
- 截图、已有自选和原始审核 JSON 仅在私有临时目录留存，不加入公开 fixture 或发布目录。

## M0 基线

最新 origin/main 与现有 production-runtime 源码均为 `31249b6bd52dca25b2ea0eb8a0497f8cc355c44a`。集成分支 `codex/ui-final-v1-20260912`，独立工作树 `.worktrees/ui-final-v1-20260912`。主目录保持原有脏改动。

原方案 ZIP：用户 Downloads 中 `ChanlunStrategy_展示优化最终方案_v1.0_交付包.zip`；16 个 SHA256 清单项验证通过。包内 HAR/截图是历史回放，不计为本次线上验收。方案正文 SHA256：`4d5e6eca6ce148dc5d00180c7979d05e423e09b8cf835b42e9d604c8816544cc`。

基线命令使用 `/usr/bin/python3 -m unittest`：unified_workbench_frontend、unified_workbench_contract、chart_centered_decision_workbench、candidate_basic_info、recommendation_chart_evidence、report_view_model、report_generator、preclose_frontend、stage_report_asset_version_updates。296 项通过，退出码 0。测试生成报告仅写临时目录。

固定输入三视口（1440×900、1366×768、390×844）首页与详情截图已保存在私有验收目录；浏览器无 pageerror，无页面横溢。重新复现搜索清空后选中 002600 但详情长度 0、全部 25 只比较为空、缩放 61—78 切结构后回到 60—79 且实例重建。测试浏览器只读 file 报告，外部请求全部阻断，唯一图表依赖来自固定版本本地缓存；浏览器运行后关闭，没有启动 HTTP 服务。

保护清单包含 89 个数据/配置文件、19 个 HTML 入口（其中 13 个含 bootstrap）、5 个实际共享数据库/账本文件；共享行情库约 1.57GB，读取前后 stat 一致。证据位于本机 `/private/tmp/chanlun-ui-final-20260912/baseline/`，不进入公开发布目录。发布后仍需对照这些保护对象重新核验。

## 完整任务与分批验收

| 任务 | 本次必须实现的范围 | 批次/验收 |
|---|---|---|
| U01 | 集合、选中、详情/图表生命周期统一；无结果恢复、过滤/键盘/手机与延迟回调一致 | M1；AC01/02/07/18 |
| U02 | 比较接入完整匹配集合与来源策略，不局限前 20 行、不虚构合并动作 | M1；AC02/03/04 |
| U03 | 身份、结构化条件、证据质量、风险口径分开，未知不等于满足 | M1；AC04/06 |
| U04 | 缺图紧凑提示；可信量能/均线元数据和序列通路，原始数据/策略不变 | M1+M2；AC10/11/12/14/25 |
| U05 | 同快照缩放保持、图层叠加、20/60/全部/信号/重置；可信几何结构按能力展示 | M1+M3+M4；AC08/09/13/14 |
| U06 | 身份和邻近短判断、阻碍/下一确认/失效条件；一步到证据或图上位置 | M3+M4；AC07/14/15 |
| U07 | 本期摘要前置、同页锚点、真实空选与观察入口，完整大盘直接可见 | M3；AC05/16 |
| U08 | 三行紧凑候选、统一蓝白样式，行情日期价基清楚，不裁风险 | M3；AC17/26 |
| U09 | 手机工具/状态分行、可读价位、正常滚动、焦点与跨视口挂载 | M3；AC18/26 |
| U10 | 我的关注入口与紧凑列表、选中展开、公共方向说明去重；私人身份独立 | M3；AC19 |
| U11 | 已有变化计数下钻、来源/阶段/版本可比边界及有证据的个股时间线 | M4；AC20/24 |
| U12 | 市场/板块/个股关联、筛选反馈与清除；研究事实不能冒充正式市场事实 | M3；AC02/16 |
| U13 | 最多三只轻量差异比较，来源合同独立，过滤后保留/标注选择 | M4；AC04/21 |
| U14 | 文字/列表与图表库独立就绪、延迟/失败/重试降级、实测冷/热缓存 | M5；AC23 |
| U15 | 复用历史验证入口，样本区间/成熟度/缺失/收益口径准确 | M4；AC22 |
| U16 | 按自然边界维护、可访问性、固定截图、源/发布资产与版本一致、回滚 | 全批次；AC24—27 |

M2 不因历史源缺证据阻塞其他批次；M4 不要求创造分钟或几何结构数据。缺能力保留原因，但不删掉当前已有能力应实现的功能。额外补充 15/30 分钟证据频率错配负例；未取分钟不能标确认失败。

## 影响边界 B01—B10

B01 不触发历史自愈/扩量，只保留缺口；B02 价格与量能尺度可追溯；B03 未评估/缺数据与无确认分开；B04 各来源正式动作与研究身份保留；B05 事件日期/取消/失效/到期不改；B06 不伪造均线、量能、分钟确认事实；B07 资格/正式评分与顺序不改，页面查看排序标明；B08 预跑/正式历史快照严格分离；B09 不写正式数据和通知，仅受控发布展示；B10 固定输入、真实负例、审查/测试/上线证据与遗留记录。

## 回收与发布流程

每批先检查实际 diff 对照需求，再检查代码质量、复现关键负例和相关回归；主进程不能只依赖 agent 报告。提交前同步最新 origin/main、确认祖先并重新回归。冲突由实现 agent 修正，主进程核对，不覆盖其他人的更改。

源码与生成资产通过既有受控流程同步。A 类逐入口完整 bootstrap hash 相等；B/C 类白名单之外相等。发布前记录源码、数据、账本、数据库保护证据；发布后读回真实根页/当期日期页/历史日期页与资源版本，三视口及关键交互验收。不得运行生产日报为 UI 刷新。

回滚使用本批逆向源码/资源改动并保留之后新增日报、数据和 outbox，不用旧快照覆盖新报告。任务结束关闭本任务启动的浏览器/临时进程并确认释放；不停止已有业务服务。

## 执行状态与卡点

2026-09-12：M0 分支、296 项基线回归、三视口截图/原问题复现与保护清单完成；状态、图表、数据三个实现任务已按 Sol / xhigh 派发。缺失场景的额外 fixture、实现回收、M3—M5、main 合并及线上验收尚未完成。

首次派发时尚无达到 5 轮上限的子问题；后续状态见下方回收记录。各实现批次分别记录轮次与证据，本文件在最终交付汇总所有未解项。

### 首批回收与独立验收

状态实现提交 c5cbc799、3968ccb1、e40fdbac；图表实现 255e7d4f、fdbe7763；数据实现 7abe6164、711cf2a9、8f40efc5。均在独立分支完成，按 origin/main 同步和祖先要求检查后回收，未发布。

- U01/U02/U03：原搜索恢复已在真实浏览器通过，详情从 0 恢复为有效内容；完整比较有 25 个对象。第 3 轮修掉手机“唯一正式动作”和旧视图回退绕过筛选；第 4 轮修掉实际搜索/板块事件未同步刷新比较的问题。独立审查相关问题均有最小修正与事件路径负例。
- U04：当前旧快照的 25 只量能元数据仍缺失，保留缺图提示；不从数值猜单位、来源。未来生成器传递可信元数据与带算法/价基说明的展示均线；显示 21 根量比与既有质量评分路径分开。均线子问题第 5 轮已拒绝布尔值、非有限价格、全空源序列和显式价基冲突，主进程复现确认，停止继续扩展此子问题。
- U05/U09：同一股票同快照缩放 61—78 后切结构仍为 61—78，实例复用；重新挂载时检查真实 DOM 归属并恢复窗口。合并时抽屉关闭先存窗口，按实际图表归属清理，不误销毁桌面图表。第 4 轮移除无窗口原点的整数索引映射与普通 high/low 区间冒充中枢的回退，只画可信日期和显式 ZG/ZD。
- 首次组合完整回归 2232 项，1 项既有跳过，退出 0；三视口无 pageerror 与页面横溢，搜索/完整比较/切图层窗口用真实浏览器复测。最后几何最小修正后的提交前回归另留日志。
- 13 个已有报告 bootstrap 与 89 个数据/配置文件不变。固定 2026-09-11 输入重新投影，仅出现 553 个允许的均线与 display_volume_ratio20 展示字段差异，无其他字段差异，输入对象不被修改；未对正式历史产物执行这个再投影。5 个共享数据库/账本文件 stat 未改变，最终交付还会完整回查。
- 线上原版用同机 Chrome 实测两组冷/热缓存：候选可操作观察约 8.1—8.8 秒，ECharts 约 8.0 秒。无请求路由的真实缓存测量保存在私有基线目录；最早使用路由、导致 HTTP 缓存失效的试测单独标注，不能当热缓存数据。

局部代码与隔离生成资源已经整合；尚未合并 main、尚未上线。M3 阅读与布局、M4 比较/复盘、M5 非阻塞加载和额外浏览器合成场景仍继续推进。当前无因五轮后仍无法解决而放弃的代码子问题；历史量能来源缺失与结构几何缺口作为数据能力限制保留。

### 第二批独立验收与修正记录

M3 已回收 46766c10、0f84d876、37bff86e；M4 已回收 a68614db、ffc71c06、bfdd0bec。主进程逐块核对冲突，冲突由原实现 agent 修正，未覆盖完整 JS。独立浏览器验收工具提交 4aa90c6d 已通过 5a46efe6 接入；工具中后续发现的旧来源标签断言、错误 pending 描述另作校正，不能把保存、推送、写库当成待做要求。

- M3 手机实看已由四段判断在图前改为一条核心判断 → K线 → 其余判断。1440/1366/390 首页能找到日期、正式空选结论和候选入口；六指数及情绪依据仍直接可见。两个重复版本入口已合为一个。
- 真实三视口再次通过搜索恢复、完整 25 只比较、缩放窗口保持，无 pageerror 与横向溢出。手机关闭返回原候选焦点，隐藏抽屉 canvas 清零。比较选择 3 只后第 4 只明确拒绝，过滤无匹配后原 3 只保留并标记集合外。
- 主进程没有用这些通过项代替全面验收：实测普通桌面候选行仍约 187.7px，未达到 U08 目标；axe-core 4.13.0 发现比较选择区语义、事实 dl 中按钮角色、比较/关注区域对比度缺陷，均交回定向修正。
- 独立审查发现 U06 信号定位误用顶层日期数组，U15 历史入口只有文字而没有实际点击路径；按既定要求补通当前图表轴日期及现有历史证据入口，不新增历史统计。
- U11 第二轮修正嵌套上一快照身份解析、错日期/阶段响应拒绝、旧候选合同保留。第三轮继续处理未声明身份被误算为可比及现有变化计数的点击路径。旧快照字段不足时保留来源事实，明确价格/升级不可作已验证比较。
- 本批定向回归 28 项通过；该结果不包含仍在修正的布局/入口/加载验收。数据保护复查完整通过：89 个数据/配置、13 个 bootstrap 字节一致，5 个数据库/账本的 SHA256 与 stat 均不变。

M5 正在验证 10 秒延迟、失败、双击重试和两张图共享加载恢复。所有浏览器均在 finally 关闭，只读本地/拦截式验收，无 HTTP 服务、日报重跑或外部写入。整体验收、提交前全量回归、main 合并和真实线上回读仍待完成。

### 发布前收口验收

最后回收：M3 b1af689c、18e35899；M4 2fe465a6、4af2d87b、b0a1ebc2；M5 0ab00599、e6d0e0a5。已统一源/发布资源及 14 个 v2 入口，资产版本 `c18ea0962bdc`。旧历史修复脚本的冻结 `APPROVED_ASSET_SHA256` 清单保持原值，没有借 UI 发布扩大历史修复审批。

主进程运行完整 `unittest discover -s tests`：2266 项、38.807 秒，`OK (skipped=1)`。最终固定输入投影仍仅 553 处已允许的展示字段差异，输入未修改。完整保护回查再次确认 89 个数据/配置文件、13 份原始 bootstrap、5 个数据库/账本 SHA256 和 stat 均未变化。

| 验收项 | 发布前实际证据与结论 |
|---|---|
| AC01—AC07 | 搜索恢复、集合/计数/比较、多来源独立合同、空正式池、研究身份和延迟切股分别由真实报告与合成负例验证；不把正式缺价改成研究或把未知改成满足。 |
| AC08—AC09 | 同快照缩放保持；最近20/60/全部/定位/重置使用实际已有日期，不补假K线。 |
| AC10—AC14 | 完整、部分、缺失、错位量能，展示均线数值与价基冲突，显式结构端点、来源价位与15分钟错配保留对应正负例；旧快照缺能力明确降级。 |
| AC15 | 主进程在1440和390实际点击价格入口，定位module02；点击历史入口直接展开module08，所有折叠祖先打开。信号日期使用当前图表轴并拒绝不匹配日期。 |
| AC16—AC19 | 三视口首页摘要/日期/入口可见；完整大盘正常文档流；主进程实测普通候选105.6875px。行情共用口径可见，例外逐条显示，风险不截断；手机核心判断→图表→其他证据、关闭返回原焦点、隐藏canvas清零。 |
| AC20—AC22 | 真实移出股票只在点击后GET上一交易快照，真实记录保留来源事实，错日期响应拒绝；三只比较上限与筛选后保留选择；历史验证/榜单/模拟入口复用已有内容和原成熟度门槛。单条来源身份/价基冲突也标为未核验与不可比，最终独立review的P1已关闭。 |
| AC23 | 主进程最终组合复跑三视口10秒延迟、失败、快速双击重试、手机空闲等待库：文字/候选可用，只挂载当前有效节点，正常加载1次、失败后重试总计2次，两图恢复。`file://`限制见下方停止项，未隐瞒为通过。 |
| AC24—AC26 | 历史载荷保持原样，缺新字段降级；源/发布字节一致。HTTPS同源真实报告回放，1440/390首页和详情axe-core4.13.0无确定违规；渐变背景/隐藏内容的incomplete继续用截图和真实可见状态人工核对，未用自动扫描替代视觉审查。 |
| AC27 | 此记录截于发布前。main合并、Pages构建、根页/日期页资源与真实线上三视口、运行目录提交读回，必须由主进程在发布后单独核验，不能由以上本地结果推断。 |

浏览器合成验收中曾出现一条工具误报：要求整数参考价必须显示为`10.00`，而既有展示格式为`10`；工具修正只按同来源合同核对等值数字，并拒绝空值被`Number(null)`误当0，不修改产品价位或删减断言。实际源日期/策略身份仍必须逐项匹配。

### 五轮停止项与保留的数据能力限制

1. **M5图表加载：第5轮后停止修改。** 手机首屏卸载节点的异步初始化问题已解决，主进程最终负例通过。仍有Chrome直接用`file://`打开HTML时的SRI/CORS兼容问题：本地ECharts被浏览器完整性策略阻止，图表区域显示明确加载失败，文字和候选仍可用。此项未继续第6轮，不以移除生产完整性校验绕过；网站HTTPS发布路径单独验收。
2. **旧快照量能元数据缺失。** 现有2026-09-11快照的25只缺少所需单位/原单位/来源序列，保持缺图提示。新生成通路具备传递能力；本次没有回写旧报告或扩量补数，不能宣称历史量能已补齐。
3. **结构及历史能力不足。** 没有可信时间端点不画笔/线段/中枢区域；缺版本、价基或更早快照时保留缺口，不伪造首次出现、升级、收益改善或完整时间线。

轮次收口：状态U01—U03第4轮；数据均线第5轮；图表几何第4轮；U06第4轮、U08第2轮、U09布局第2轮、U16无障碍第1轮；U11第4轮、U13第2轮、U15第4轮；M5第5轮。没有用重新命名同一产品问题重置轮次。除上述停止项和数据能力限制外，已发现的阻断缺陷均完成定向修正和主进程复验。

提交前再次同步 origin/main（仍为31249b6b）并确认祖先关系；同步后299项相关回归通过。主进程独立复跑最终合成浏览器验收为PASS，三视口无pageerror/console error，候选完整合同、图表窗口、15分钟错配、旧版降级和手机往返均通过。M5最终组合5个模式也全部通过，包含手机空闲无detached实例及点击/关闭路径。发布前遗留仅为上方明确记录的file协议兼容和数据能力缺口。

### 2026-09-13 线上验收后的M5撤回决定

此节替代上文发布前对M5可上线的判断，保留原测试记录及失败证据，不把局部通过改写成完整验收。

PR #11合并为`b6d7b6a79c62a66840f618b0508914501c2315f6`，Pages构建34704241723成功；主进程读回根页、9月11日页、8月14日页、三份资源及两份JSON，字节与已验收版本一致，运行目录也已快进到同一提交。但真实HTTPS浏览器交互未通过：1440桌面根页打开“全部”，在库尚未就绪时再次点击已选000636，ECharts已ready而个股仍停留“加载中”，30秒内没有实例。原池有50根完整OHLC，不能归因于缺K线。

原因是重复选择递增selection version，但同股详情复用直接返回，原pending回调被丢弃后没有新挂载。原合成测试覆盖了切换另一只股票，遗漏重复点击同一只的路径。手机detached实例问题与此不同，e6中已通过的手机清理仍保留。

依照每子问题五轮上限，**停止M5非阻塞加载，不做第6轮修复；撤回到原先库先加载、业务后初始化的方式**。本次不承诺U14/AC23非阻塞目标已经实现，性能改善结论也撤回。搜索、完整比较、同股窗口、图层、量能/均线展示通路、紧凑布局、行情口径、手机详情、证据入口和历史合同核验继续保留。

撤回分支为`codex/ui-final-v1-loading-isolation-20260913`。M5专属完整测试与浏览器断言归档到`tests/fixtures/ui_final_browser/archive_m5_loading/`，保留旧提交复现指引，不删除失败材料或降低断言。主验收工具及M1—M4负例继续执行。首次发布资源证据、浏览器失败、旧5模式局部通过材料分别留在私有`live-artifacts-first.json`、`live-acceptance-first-failed/`、`final-loading-review/`。

撤回后的main合并、Pages发布、运行版本和真实网页验收需要重新完成，不能沿用PR #11的发布状态声称最终交付通过。最终未完成项包括非阻塞加载（同股重复点击生命周期及file协议SRI兼容未继续优化），以及原有量能元数据、可信结构端点和历史身份不足；后三类保留真实缺口，不追加补数。

撤回发布前复核：2262项完整回归，36.554秒，`OK (skipped=1)`；主进程三视口固定fixture PASS。1440/390真实报告HTTPS回放再次通过约105.7px候选密度、比较上限与筛选保留、价格module02及历史module08实际点击、手机关闭焦点和隐藏canvas清理，axe无确定违规。89个数据/配置、13份bootstrap、5个数据库/账本哈希仍未变化。两个归档文件仅调整相对根路径以适应归档位置，原断言保留。

独立最终审查可提交、无阻断发现。14个v2入口（根页、比较页、12个日期页）已恢复原加载顺序，统一资源版本为`6d968e0ef582`；M1—M4与手机首屏清理保留。U14/AC23非阻塞目标仍明确停止，不能把恢复原加载方式记作该目标完成。
