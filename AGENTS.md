# ChanlunStrategy 项目修改约束

## 选股相关改动必读

修改行情获取／补齐、资格与召回、信号／确认、评分／排序、观察跟踪、预跑／盘后、报告或推送前，先读取 `docs/plans/2026-09-10-preclose-selection-quality-repair-plan.md` 第 12—14 节及涉及的设计／验收章节；第 14 节是本任务最新授权，历史执行记录不能覆盖它。

- 按 B01—B10 说明本次影响的边界，检查实际工作树与运行版本；禁止用旧目录覆盖已上线保护。
- 已确认路线是先修正确性、再扩观察覆盖、新路径验证后再决定进入正式主推。方案已确认不等于修复已上线，也不是任意写库／部署授权。
- 历史数据缺口在授权预算内补齐复查；失败保留待补齐，不能长期作为策略拒绝。用户要求停止历史补数或限定成本时，使用已有材料验证并保留缺口，不为追求完整覆盖无限扩量。单股与系统性故障分开，缺价基／未取分钟不能冒充破位或无确认。
- 各形态独立判断，共享数据与风险约束；不为指定股票全局降阈值，不把研究观察直接转正式。过期源遵守既定十日门，不能换日期复活。
- 判断、标签、市场事实和排序用途必须一致；保留预跑正式数据只读隔离、截止、幂等及跨入口回归。
- 允许合理优化实现、性能或策略提案。需给出原因、固定输入前后对照、正负例与相关回归；策略变化增加成组验证，不能仅用事后赢家证明有效。
- 改变正式策略身份、风险语义、既定时效或外部写入边界，先说明并取得对应授权；已授权范围内的等价改进不反复请求许可。
- 后续任务按第 12.4 节留下变更记录，并在方案演进记录追加有证据的替代关系；不得静默改回已知错误、删除负例或弱化断言。
- 提交／移交前确认本文件和方案一并携带（本地排除规则可能忽略 Markdown），保留其他人的脏改动；没有实际测试／部署证据的项继续标待完成。

## 策略回测与模型协作

- 凡改变选股范围、确认、评分、排序、参考位或进出条件，先冻结现行基线，在隔离版本完成可用数据范围内的新旧对照回测，给出优化结果、风险、缺失样本和影响范围。默认由用户确认结果后再落地；用户已明确授权主进程独立审核并上线时，主进程按该授权验收通过后执行，不再重复要求人工拍板。方案方向获批不自动等于发布授权，称为“修 bug”也不能绕过行为影响评估；数据不足不能伪造收益改善或用局部通过冒充全量结论。
- 为产生回测，允许在已授权范围内编写隔离实验代码；不得改运行目录、写正式数据库／账本／报告或发布。纯文档、展示等不改变选股行为的变更做相应回归，不强求收益回测。
- 主进程负责方案、回测设计、影响评估和最终独立验收；代码落地交给经用户确认模型配置的子 agent。主进程必须检查实际差异、复现关键结果并运行相关验收，不能仅采信子 agent 的完成报告。
- 任务开始时确认主进程与落地子 agent 各自的模型和推理强度并记录；同一任务内的研究、实现、修正、验收和分批交付沿用已确认配置，不因拆成新一轮代码批次重复询问。
- 长期规则不固定模型名称或版本；旧任务的型号不是后续默认值。模型升级、配置不明或所选模型不可用时先告知并重新确认，不静默沿用旧模型、替换或降级。

这些要求补充上级规则，不替代上级关于先方案、提交同步、生产服务禁启与任务资源清理的约束。

## 2026-09-11 本次选股优化的执行授权

- 用户明确要求停止继续扩量补历史数据，使用已有材料把能测的测完；缺失范围保留在结果中，不要求补齐4587个目标才推进。
- 本任务主进程保持当前模型和推理配置；落地子 agent 使用GPT-5.6 Sol / medium，任务内后续批次不再重复确认。该型号是本任务记录，不是后续新任务的永久默认。
- 用户已授权“结论是好的，你自己审核一下没问题，就上线”。主进程独立核对实际差异、现有数据对照、正负例、相关回归、运行隔离和上线结果，通过的范围直接落地发布；不足以证明安全有效的变更保留研究状态，不为了完成上线而放宽标准。
- 历史大规模补数与全库批量写回不在此次上线中继续推进；新增入口收益观察未优于原有组，现阶段不扩大正式主推。该授权替代本任务此前逐轮模型确认和每批结果必须再次人工确认的等待状态，原证据与历史记录保留。

## 2026-09-12 展示优化 v1.0 执行授权

- 用户授权按最终方案新建分支实现，代码由 `gpt-5.6-sol / xhigh` 子 agent 处理，不启用 Fast；主进程保持当前配置并负责回收、独立审查和验收，最终合并 main 并上线。该配置仅适用本次展示任务，不沿用前次 Sol / medium。
- 完整范围为 U01—U16、M0—M5、AC01—AC27，按 [本次实施记录](docs/plans/2026-09-12-ui-final-v1-execution.md) 分批推进；不改策略/正式排序/风险合同，不无限补历史，不重跑生产或通知。
- 每个子问题最多 5 轮实质优化；仍未解决则记录原因与影响、隔离不安全变更并继续其他任务，最终汇总所有卡住的问题。不得因此掩盖测试失败或弱化原保护。
