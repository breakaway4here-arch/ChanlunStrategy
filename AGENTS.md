# ChanlunStrategy 项目修改约束

## 长期总则

长期原则适用于后续各批工作；历史任务的方案、证据、预算、轮次和授权单独见文末，按其任务及启用范围解释。

### 日常优化：小步快跑、可用性优先（长期规则）

本项目默认采用增量优化：**保留可用能力，每批交付一个有实际使用价值、可独立验证和回滚的小目标。允许局部数据缺失，不允许为了完善非必要能力而使主要功能长期无法发布。**

本节适用于需求分析、实现、子 agent 派发、审查、测试、验收与交付；主进程与子 agent 使用同一口径。它不是允许带错误上线，也不是禁止必要的局部重构。

#### 1. 小目标先交付，不先重做整个系统

开始时简要明确本批目标、不能退化的既有能力、允许的局部降级和留待后续的增强。已有任务描述能说明的，不另写大方案。

优先修改最短必要调用链，复用已有组件和有效路径。普通展示或交互修复，不自行扩大成换框架、重建数据模型、全链路重写、统一所有历史版本或补齐全部历史数据。必要的局部抽取和适配可以做；大范围重构单列收益、最小替代方案、影响与回退路径，取得相应授权后再做。

#### 2. 保住可用版本，失败分项单独隔离

以实际发布版本和当前有效改动为基线，保留原工作区及已验证成果。新路径通过实际使用场景前，不整体替换原可用路径；不要同时混做业务增强、公共核心重构和加载机制改造。

一处受阻，优先局部修复、关闭不安全新增能力或回退该分项。已证明依赖分离的安全增量可按有效授权单独交付，不把全部成果一起推倒。不能从失败工作树盲目挑文件上线，也不能把停用本批核心目标伪装成完成。

#### 3. 缺哪项降级哪项，已有信息继续可用

成交量、均线、市值、条件等缺失时，对应字段显示“—／未记录／暂不可用”，其他内容正常展示。上期快照或连续性依据不足，只关闭相关比较，保留当前记录及能核验的变化。无法可靠合并的来源先分别展示；用途未核验的原价可带标记作文字展示，不画确定参考线、不参与相关计算。

缺失、不适用、未核验、冲突和真实零值分开；未登记风险不等于无风险，未比较不等于未变化。提示简短，不为每个字段重复堆长篇免责声明。不为填满页面编造数据或无限补数。

#### 4. 少做不可靠兜底，不让新要求挡住旧数据

基础行情读取与策略判断分开，不要求 K 线同时具备完整策略合同。使用正确报告上下文和明确引用，保留原有有效性检查；不能仅凭代码相同、日期相同或只有一条记录，就跨来源借图、合并版本或配对历史。

不能证明一致的分项允许关闭，不必为本次上线建设通用兼容系统。本来源原本有效的数据被新代码丢掉属于回归；关闭原有不可靠兜底导致个别分项留空，可作为明确说明的安全降级，不借此掩盖大面积取数失败。

#### 5. 验收匹配本批风险，不以全项目完美为前提

覆盖本批目标、直接上下游、核心冒烟，以及受影响的正常／缺失／错误输入。界面修改检查实际可见结果和交互；影响手机时实际打开手机尺寸详情验证，不靠隐藏节点或实例数量证明成功。共享核心改动扩大回归，纯文档变更不升级为生产执行或全站重测。

历史全量矩阵按本批启用范围裁定：明确延期的增强可未完成，正确局部空态可通过。现有必须执行的 CI／发布检查不绕过；过时测试按已确认需求更新并说明原因，保留正确性反例。不要只凭测试数量宣布完成，也不要只凭失败总数否决发布。

#### 6. 审查分级，阻断项必须有实际依据

区分“本批阻断／已接受局部限制／后续优化或无关旧问题”。阻断项说明实际复现、影响的启用路径和最小处置。

本批主要功能失效、重要信息错配、数据损坏或泄漏、越权副作用、发布资源与验收版本不一致，应修复或停用对应不安全路径。缺非必要字段、未启用增强、无关重构建议，不阻断独立安全的改动。

旧问题先与同输入基线对照。当前启用路径正在误导用户的已知错误，最小修复或局部关闭，不因“原来就错”放过；与本批无关的历史问题进入待办，不借机要求全站重做。

#### 7. 在明确范围和预算内完成小闭环

按“复现 → 最小修改 → 定向验证 → 实际操作 → 交付”推进。反复无进展时，核对原因、缩小范围或隔离，不机械重试，不不停增加新架构。

历史五轮／六轮／七轮及停止记录保留，但不自动成为后续所有任务的默认限制。仍有效的用户明确预算、轮次和停止要求继续遵守，不擅自取消或重置；不要自行新增“首次验收后冻结，只准报错不准修”的限制。有效授权内不逐步骤重复请求许可。

#### 8. 小范围发布和回滚，简短记录结果

本批目标、主要可用路径、已接受降级及源码／发布资源版本核对完成后，按有效授权交付，不等待后续全部增强。发布回归只撤本批代码／资源，保留之后新增报告和业务数据；不用诊断包或脱敏 HTML 覆盖真实日报，不为刷新 UI 重跑生产任务。

简要记录“本批完成、局部降级、后续待办、验证与发布状态”。本地测试和构建成功不等于已上线，部分交付不称全部完成。

#### 与历史方案及授权的关系

本节规定后续日常优化的默认组织方式与完整性口径。旧方案、失败证据和任务范围保留；整套功能清单、全历史覆盖和特定轮次，不自动成为后续每批发布的前置条件。与当前用户已确认范围冲突的旧完整性要求注明替代关系，避免同时执行两套相反要求。

既有数据安全、策略身份、正式排序、价格用途和生产隔离要求继续有效。本节不新增发布、写库、通知或策略变更权限；当前有效的任务授权与预算单独记录，不从长期规则或历史完成记录推导。

### 选股相关改动必读

修改行情获取／补齐、资格与召回、信号／确认、评分／排序、观察跟踪、预跑／盘后、报告或推送前，先读取 `docs/plans/2026-09-10-preclose-selection-quality-repair-plan.md` 第 12—14 节及本批涉及的设计／验收章节。第 12 节的策略与数据保护按影响范围继续有效；第 13—14 节的模型、预算、轮次和发布许可仅适用于所记录任务，须结合该任务后续授权核对，不能作为新任务的默认授权或完整性交付门槛。

- 从 B01—B10 中说明本批实际影响的边界，不适用项简注原因；检查实际工作树与运行版本；禁止用旧目录覆盖已上线保护。
- 已确认路线是先修正确性、再扩观察覆盖、新路径验证后再决定进入正式主推。方案已确认不等于修复已上线，也不是任意写库／部署授权。
- 历史数据缺口在授权预算内补齐复查；失败保留待补齐，不能长期作为策略拒绝。用户要求停止历史补数或限定成本时，使用已有材料验证并保留缺口，不为追求完整覆盖无限扩量。单股与系统性故障分开，缺价基／未取分钟不能冒充破位或无确认。
- 各形态独立判断，共享数据与风险约束；不为指定股票全局降阈值，不把研究观察直接转正式。过期源遵守既定十日门，不能换日期复活。
- 判断、标签、市场事实和排序用途必须一致；保留预跑正式数据只读隔离、截止、幂等及跨入口回归。
- 允许合理优化实现、性能或策略提案。需给出原因、固定输入前后对照、正负例与相关回归；策略变化增加成组验证，不能仅用事后赢家证明有效。
- 改变正式策略身份、风险语义、既定时效或外部写入边界，先说明并取得对应授权；已授权范围内的等价改进不反复请求许可。
- 后续任务按第 12.4 节留下变更记录，并在相关方案演进记录追加有证据的替代关系；不得静默改回已知错误、删除负例或弱化断言。
- 提交／移交时按“工作树、覆盖文件与子 agent 派发”核对规则文件，保留其他人的脏改动；没有实际测试／部署证据的项继续标待完成。

### 策略回测与模型协作

- 凡改变选股范围、确认、评分、排序、参考位或进出条件，先冻结现行基线，在隔离版本完成可用数据范围内的新旧对照回测，给出优化结果、风险、缺失样本和影响范围。默认由用户确认结果后再落地；用户已明确授权主进程独立审核并上线时，主进程按该授权验收通过后执行，不再重复要求人工拍板。方案方向获批不自动等于发布授权，称为“修 bug”也不能绕过行为影响评估；数据不足不能伪造收益改善或用局部通过冒充全量结论。
- 为产生回测，允许在已授权范围内编写隔离实验代码；不得改运行目录、写正式数据库／账本／报告或发布。纯文档、展示等不改变选股行为的变更做相应回归，不强求收益回测。
- 主进程负责方案、回测设计、影响评估和最终独立验收；代码落地交给经用户确认模型配置的子 agent。主进程必须检查实际差异、复现关键结果并运行相关验收，不能仅采信子 agent 的完成报告。
- 任务开始时确认主进程与落地子 agent 各自的模型和推理强度并记录；同一任务内的研究、实现、修正、验收和分批交付沿用已确认配置，不因拆成新一轮代码批次重复询问。
- 长期规则不固定模型名称或版本；旧任务的型号不是后续默认值。模型升级、配置不明或所选模型不可用时先告知并重新确认，不静默沿用旧模型、替换或降级。

这些要求补充上级规则，不替代上级关于先方案、提交同步、生产服务禁启与任务资源清理的约束。

### 工作树、覆盖文件与子 agent 派发

- 开始需求分析、派发、实现、审查和验收前，确认实际工作树／分支及本批基线，读取项目根 `AGENTS.md`、当前实施树根规则和本批相关方案。派发时传入这些准确路径及本批目标、保留能力、可接受降级、禁止项与验收范围；子 agent 先回报读取路径和规则版本，再开展对应工作，审查与实现使用同一口径。
- 实施树、子目录的 `AGENTS.md`、`AGENTS.override.md` 或其他工具约束文件若重复或冲突，先说明并按适用范围整理；更具体的策略／安全／权限保护保留，旧完整清单或轮次不得覆盖已确认的本批范围。历史工作树、失败快照和诊断包用于留证，不是新任务的规则入口，不能原样复制旧约束重新生效。
- 在当前实施树和提交树用 `git ls-files --error-unmatch` 确认本文件及本批引用的规则／方案受跟踪，并核对提交内容、文件版本和可读取性；本地 `*.md` 排除不能造成漏带。新工作树基于包含规则提交的版本；旧工作树按差异合并规则，不覆盖产品改动。已运行代理不会因文件更新自动重新读取，继续派发时须明确要求重读。

## 历史任务与授权记录（按范围适用）

以下保留旧方案与授权的演进，不从某次“完整任务清单”推导后续每批前提，也不从历史发布许可推导新权限。仍有效的用户预算、停止要求和安全边界按对应任务继续遵守；后续明确授权只替代其写明的范围。历史测试或失败数只证明当时版本与场景。

### 2026-09-11 本次选股优化的执行授权

适用范围：2026-09-10 发起的选股正确性优化任务及明确延续的批次；不授权新的策略扩展、写库或发布。

- 用户明确要求停止继续扩量补历史数据，使用已有材料把能测的测完；缺失范围保留在结果中，不要求补齐4587个目标才推进。
- 本任务主进程保持当前模型和推理配置；落地子 agent 使用GPT-5.6 Sol / medium，任务内后续批次不再重复确认。该型号是本任务记录，不是后续新任务的永久默认。
- 用户已授权“结论是好的，你自己审核一下没问题，就上线”。主进程独立核对实际差异、现有数据对照、正负例、相关回归、运行隔离和上线结果，通过的范围直接落地发布；不足以证明安全有效的变更保留研究状态，不为了完成上线而放宽标准。
- 历史大规模补数与全库批量写回不在此次上线中继续推进；新增入口收益观察未优于原有组，现阶段不扩大正式主推。该授权替代本任务此前逐轮模型确认和每批结果必须再次人工确认的等待状态，原证据与历史记录保留。

### 2026-09-12 展示优化 v1.0 执行授权

适用范围：该次展示优化任务。完整性口径随后由 2026-09-14 可用性范围替代；轮次变更按下方该任务后续记录衔接，不能迁移为其他任务的五轮上限。

- 用户授权按最终方案新建分支实现，代码由 `gpt-5.6-sol / xhigh` 子 agent 处理，不启用 Fast；主进程保持当前配置并负责回收、独立审查和验收，最终合并 main 并上线。该配置仅适用本次展示任务，不沿用前次 Sol / medium。
- 原任务完整清单为 U01—U16、M0—M5、AC01—AC27，保留用于追踪；每批只验收实际启用及受影响范围，不要求整份清单同时完成。按 [本次实施记录](docs/plans/2026-09-12-ui-final-v1-execution.md) 分批推进；不改策略/正式排序/风险合同，不无限补历史，不重跑生产或通知。
- 每个子问题最多 5 轮实质优化；仍未解决则记录原因与影响、隔离不安全变更并继续其他任务，最终汇总所有卡住的问题。不得因此掩盖测试失败或弱化原保护。

### 2026-09-14 可用性发布与限定第七轮授权

适用范围：展示任务 R04/R06/R08 的限定第七轮。以下为当时授权与状态，范围内停止限制已由后文“三个收口点继续实施授权”替代；原失败证据和轮次保留。

- 用户明确以“主要功能可用、局部缺失不影响整体”为本次上线标准，替代与之冲突的完整性交付要求。允许字段/模块内缺失降级；已有数据继续展示，未知不得写成 0、无风险、未变化或条件满足。
- 原 R04/R06/R08 已各完成 6 轮并停止；用户现明确回复“授权这个限定第 7 轮”，限恢复既有取图与主要详情入口、多来源分别展示、关闭不可靠比较。保留前六轮全部记录，不因新分支或收敛范围重置；累计上限 7，不暗开第八轮实质修正。
- 实现继续本次展示任务已确认的 GPT-5.6 Sol / xhigh、Fast 关闭；主进程保持配置并独立验收。失败第六轮树继续隔离，新候选从实际发布基线建立，不能把已知丢数据/错标的/错日期/有图变无图作为正常缺失发布。
- 不要求复杂自动合并或全部历史计算先完成；无法核验的记录按来源原样保留，相关比较/计算不启用。原 40 条继续记录，明确启用、降级与未启用范围，不能把未启用增强能力写成通过。
- 主要流程正常、已展示事实正确、缺失隔离、源/发布资源一致后按现有有效授权合并上线；不再逐步骤询问。不改策略、正式数据/顺序、通知，不扩大历史补数，不恢复 M5 加载实验。计划与边界见 `docs/plans/2026-09-14-ui-availability-release.md`。

#### 第七轮停止记录

记录时点：第七轮首次验收后、三个收口点继续实施授权前。以下“冻结／只能只读”不作为后续所有批次的默认限制。

- 限定第七轮已经实施并首次验收，累计 7/7，候选产品冻结。启用路径仍存在同日异价基借图及用途未核验合同价被画为正式参考线，不能按局部缺失放行。未合并、未发布、未追加第八轮产品修正。
- 本记录只追加到第七轮隔离树；第六轮原记录与用户脏树不修改。后续只能继续只读证据/报告工作，任何新的实质修正需对应明确授权。详见 `docs/plans/2026-09-14-ui-availability-round7-result.md`。

#### 2026-09-14 三个收口点继续实施授权（替代上述范围内停止限制）

适用范围：本次展示任务列出的三个收口点，详见 [继续实施记录](docs/plans/2026-09-14-three-closures-release.md)。这是该任务的既有授权记录，本次规则文档整理不执行或扩大它。

- 用户明确授权保留第七轮手机图表、主要入口、关闭重开及移除操作，仅继续：按当前 ref/对应报告取本来源 K 线，关闭跨来源自动借图；用途未核验原价只带说明展示，不画正式参考线、不计算；事故复盘关闭参考线后补原值文字替代。
- 原七轮及失败记录保留；后续实质修改从累计第八轮继续记载，范围内必要修改/测试/独立审查无需每次另请轮次授权。不新建价基兼容/复权换算，不扩成全历史字段改造；扩大功能、补数、改变策略或显著增加资源另行说明。
- 配置沿用本次展示任务 Sol/xhigh、Fast关闭，主进程独立验收。允许更新过时测试，但错来源、错价格用途、原值可读性保护不删除。正常取图、异价基局部空态、未核验原价无参考线、原值可读和主要操作通过，资源与数据保护核对后按有效授权上线。
